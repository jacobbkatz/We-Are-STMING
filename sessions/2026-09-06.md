# Session 2026-09-06

**Who was there:** Jacob, remote. No bench access.
**Type:** code only
**Goal:** answer "what are you still confused about", and chase one schematic detail while doing it

---

## 1. Headline

| | |
|---|---|
| **CLEAR# and RESET# are floating on all four DACs** | Marked no-connect on the schematic. Four floating active-low resets glitching together is the best explanation yet for the DAC configuration loss. **Unverified against the datasheet** |
| **The 37 nA candidates can be ranked by impedance** | A ±15 V rail leaking to the input needs 405 MOhm — typical of contamination. A 0.5 V galvanic cell needs 13 MOhm — implausibly low for a surface path. **This favours contamination over the floating shield as the DC offset source** |
| **A new discriminating test falls out of that** | If the offset is rail-to-input leakage it should scale with rail voltage. Drop ±15 V to ±10 V and it should fall by a third |
| **The ADC's output format has never been verified** | The firmware reads `int16_t`. If the LTC2326-16 is straight-binary, every reading above 32767 reads as negative and the "-400 baseline" is really 65136 |

No measurements. All analysis.

---

## 2. The DAC configuration loss: a real lead at last

Zoomed into schematic page 1 at the four AD5761 symbols. **Every one of them carries a green
no-connect cross on pins 2, 3, 9, 10 and 11:**

| Pin | Name | Consequence |
|---|---|---|
| 2 | **CLEAR#** | active-low input, **floating** |
| 3 | **RESET#** | active-low input, **floating** |
| 10 | SDO | no DAC readback — consistent with H1 carrying no MISO |
| 11 | LDAC# | floating, but unused: the firmware writes with `CMD_WR_UPDATE_DAC_REG` |
| 9 | DNC | correct, do-not-connect |

**Why this is the best hypothesis so far.** A floating CMOS input sits at an undefined potential
and can be driven low by coupled noise alone. Four of them, on one board, in one noise
environment, would glitch **together** — which is the defining feature of this fault and the thing
the rail-dip and thermal theories never explained. A RESET# assertion would clear the control
register, kill the outputs, and be undone by `RSET`. That is the symptom exactly.

**Two things must be checked before believing it:**

1. **Does the AD5761R have internal pull-ups on CLEAR# and RESET#?** If yes, floating is far less
   dangerous and the hypothesis weakens a lot. **Datasheet question. Nobody has looked.**
2. **Are they actually open on our board?** Meter continuity from U1 pin 2 and pin 3 to 3.3 V.
   Thirty seconds with a continuity beeper.

**If confirmed, the fix is four short wires** tying CLEAR# and RESET# to 3.3 V — a permanent fix
rather than the periodic re-arm workaround discussed on 2026-09-01.

## 3. Ranking the 37 nA candidates by impedance

The preamp's input is a **virtual ground**. For surface leakage to inject current there, the
current is whatever voltage sits at the far end of the path, divided by the path resistance:

| Source at the far end | Resistance needed for 37 nA | Plausible as a surface path? |
|---|---|---|
| A ±15 V supply rail | **405 MOhm** | **Yes.** Very typical of a contaminated surface |
| A ~0.5 V galvanic cell on the shield | **13 MOhm** | **Low.** That is a poor insulator, not a dirty one |

**This favours the cyanoacrylate and flux hypotheses over the floating shield** as the source of
the DC offset. It does not clear the shield — a floating shield is a genuine noise problem and
still needs grounding — but the arithmetic says a rail leaking to the input node across a dirty
surface is the more likely offset mechanism.

**The recommendation does not change.** Ground the shield first: it is one reversible wire, and
the rebuild consumes the only spare board.

**But a new and cheaper test now exists.** If the offset is rail-to-input leakage it should
**scale with rail voltage**. Drop the supply from ±15 V to ±10 V and the offset should fall by
about a third. If it does not move at all, the rails are not the source and the ranking above is
wrong.

## 4. Corrections to earlier documents

| Where | It says | Correct |
|---|---|---|
| `STATUS.md` fault 4 | DAC config loss cause unknown; U16 not hot weakens thermal shutdown | Still true, but there is now a **leading hypothesis**: floating CLEAR#/RESET# |
| `STATUS.md` fault 1 | Three candidates for the 37 nA, none distinguished | Still three, but they can now be **ranked**: contamination is more plausible than the shield on impedance grounds |
| `sessions/2026-09-01.md` §2 | The galvanic cell "is entirely plausible" for 37 nA | **Weakened.** It needs a 13 MOhm path, which is low for a surface leak. Recorded then as inference, and it is now a less likely inference |

## 5. What is still genuinely unresolved

Recorded because the question that prompted this session was exactly that, and the answers belong
in the repository rather than in a chat.

**Biggest, because it changes the meaning of every ADC number:**

- **Is the LTC2326-16's output signed two's complement or straight binary?** The firmware reads
  `int16_t`. If the part is unipolar straight-binary, readings above 32767 wrap to negative, and
  the "-400 count baseline" is really 65136 — near full scale, not near zero. Every derived
  current, the 37 nA included, depends on this. **Datasheet question, never checked.**

**Load-bearing and unverified:**

- **Is the firmware in the repository what is actually flashed?** All the firmware analysis assumes
  it is. If Nuh flashed something never pushed, the `CCON` and motor findings could be stale.
- **Does our disc have the same ~700 nm Z range as Berard's?** His is a 20 mm disc; ours is
  25–27 mm. Bigger discs generally move further. Our figure is borrowed, not measured.
- **Our lever ratio** — still VERIFY. The geometry is inside `.f3d` files this review could not
  read.
- **Which Z direction and which motor sign advance toward the sample** — both still unknown, and
  `stm_approach.py` refuses to run without them.
- **`stm_approach.py` has never touched hardware.** 40 tests against a simulation is not a bench
  test.

**Waiting on other people:**

- **Mech Panda's licence.** No LICENSE file, so all rights reserved by default. Needs asking him.
- **Whether the DAC loss is startup-only or recurring** — Jacob and the 2026-08-31 log still
  disagree, and it decides how often LED1–LED4 must be checked.

## 6. Process note

The finding in section 2 came from zooming into a schematic that had already been read once, in
text form, the previous day. **The text extraction gave pin names but not connectivity** — the
no-connect crosses are graphical and invisible to text extraction. Reading the same document a
second way produced the lead.

That is now twice in two days that a source already in hand yielded more on a second look.
