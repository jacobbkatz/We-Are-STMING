# Session 2026-09-21

**Nothing powered. Nothing touched. No measurement taken.** The instrument is still disassembled
and moved. This was a website session, run by Claude with Jacob directing, and its headline is that
**an independent audit of the public page found twenty-one claims that did not survive a check
against this repository — six of them written by Claude earlier the same night.**

---

## 1. Headline

| | |
|---|---|
| **THE PAGE CLAIMED WE WROTE THE FIRMWARE. WE DID NOT** | It said *"every line of code here was written by Claude — the firmware, the analysis..."* and, separately, *"we wrote the firmware"*. **The firmware is Mech Panda's.** `README.md` line 10 says so, `Code/teensy/src/main.cpp` still carries his typo `// Serial Commnications`, and the page credited him 200 lines further down. **The page contradicted itself and nobody had noticed.** We adapted his firmware; both sentences now say that |
| **A NANOMETER GAP WIDTH IN THE HEADLINE, WHICH OUR OWN FRAMING FORBIDS** | The hero read *"electrons crossed a gap about a nanometer wide"*. `FRAMING.md`'s list of what the canonical sentence deliberately does NOT say has, in terms: **"a distance in nanometers from our own hardware — the scale has never been established here."** The width is gone |
| **A BOUND STATED BACKWARDS, IN TWO PLACES** | *"One step moves the tip AT LEAST eleven times the width of the window."* `docs/FACTS.md` says tip travel per motor step is **UNDER 1.94 nm**, from a lever ratio of **OVER 80** — so eleven times is the **ceiling**, not the floor. Smaller `d` gives a larger ratio and a smaller step. **`sessions/2026-09-20.md` §3.3 has the same inverted wording**; it is history and keeps it, but `docs/FACTS.md` is the register and it says UNDER |
| **A SUSPECT WAS BEING SOLD AS A REMEDY** | The coin wrappers were described as *"damping the springs"*. `docs/INVENTORY.md` records them as **MASS** — three rolls, 18 quarters each — added to push the springs past their initial tension. The damping is **eddy-current**, from magnets over an aluminium plate. Worse: `STATUS.md` lists the coin mass as **candidate 6 of 7** for the gap motion that is the page's own first blocker |
| **THREE FACTS ABOUT NUH THAT ARE IN NO FILE IN THIS REPOSITORY** | The page said Laces Up *"collects, cleans and redistributes shoes"*, is *"now in its fourth year"*, and that he *"has taught STEM to more than twenty students between five and sixteen"*. **None of the three appears anywhere.** `sessions/2026-09-20.md` §12 is the only record of either profile and says none of it. Removed |
| **THE PAGE WAS NOT USABLE WITHOUT A MOUSE** | The mobile menu sat before its own button in the DOM, so opening it put eight tab stops **behind** the focused button and forward-tabbing never reached them. The 3D model's part information was bound to `pointerup` alone, and **19 of its 20 part names appear nowhere else on the page** |

---

## 2. What was done

~~**A new section, second on the page: "Did it work?"** The instrument broken into twelve
subsystems, each with a verdict and the measurement behind it. Four finished and verified, five
working but never calibrated or never made to hold, three never demonstrated.~~ **BUILT, PUSHED,
LIVE FOR ABOUT AN HOUR, THEN REMOVED THE SAME SESSION — Jacob's call, having seen it on his phone:
*"I don't like this new section you added remove it."*** It is in the history at `4c007c9` if
anyone wants to look at what it was. **The checking behind it was not wasted and is the reason
most of this log exists** — every row was verified against `docs/FACTS.md`, `STATUS.md`,
`docs/INVENTORY.md` or the session log that recorded it, and that pass is what turned up the
firmware-authorship error, the inverted motor-step bound and the coin-wrapper mix-up, all of which
were on the page independently of the section and all of which are fixed.

**What went with it:** its nav link, its footer link, its CSS, and the hero link that pointed at
it, which now points at what stopped us.

**US English** across the page, the thirteen figure scripts (re-rendered), the poster and
**31 deliverable documents — 443 substitutions**, verified mechanically as spelling-only: 417
removed lines, 417 added, and a canonicalizing comparison over every changed pair finds no content
difference. **`sessions/` was not touched.** `docs/`, `STATUS.md` and `CLAUDE.md` were deliberately
left in British spelling — see §6.

**The tunneling section became the argument rather than a summary of it**: the four physics steps,
including why field emission and thermionic emission are excluded at half a volt across a
nanometer, then four measurements that close the metal-bridge escape route. And a new
**"What this does not say"**, which puts the two numbers that point *away* from a clean vacuum gap
on the page: **16 to 59 MΩ is at the low end for STM tunneling**, which usually runs from about
100 MΩ up, and **V^1.55 is steeper than an ideal gap at these biases**.

**Figure 2's caption said the junction was "on gold".** It is the 2026-09-17 junction, and Jacob's
own note that night was *"oh ya I think your prooabbly over copper"*. `sessions/2026-09-17-bench.md`
§3.27: *"Nothing measured tonight can tell the two apart."* The caption now says so.

**Repetition.** Six ideas the page stated more than once are down to one telling each. *"What we do
differently"* was four paragraphs restating the register, the checker and the control run; it is
one sentence now. The About section had been **18.4% of the page**.

---

## 3. Two checkers gained a test, and both were proven red-then-green

| Check | What it caught the moment it was pointed at the old code |
|---|---|
| **`check_site.py` — spelling** | Any British spelling on the page, **including the inflections a bulk substitution of the base word leaves behind.** Converting `tunnelling` had left **`tunnelled` standing in the central claim of the tunneling section**, and `grey` in a comment. The new check immediately caught two more in a comment written minutes earlier |
| **`audit_live.py` — the nav bar** | The bar must stay one row and be no taller than the `scroll-margin` written for it. Red: *"the nav bar wraps onto 2 rows"* and *"scroll-margin-top is 68 px but the bar is 82 px, so an anchor lands under it"* |
| **`audit_live.py` — SVG label size** | No SVG label may render under 12 px **once the viewBox scale is applied**. Red: **19 labels**, at 8.4 to 10.7 px |

---

## 4. Measurements taken on the page itself

| | |
|---|---|
| **SVG labels, smallest anywhere** | **8.4 px** before, **12.0 px** after, swept across **22 viewport widths from 320 to 1680 px**, including every band edge |
| **Model contrast in dark mode** | **81% of the model's pixels fell under 3:1** against the dark ground, because the three group colours were hard-coded in the fragment shader. They are CSS tokens now, with dark variants |
| **Gallery weight** | **2,928 KB** of full-resolution JPEG to display thirteen charts at 233 px. **341 KB** after, an 88% saving. First view is unchanged: 5 requests, 383 KB, LCP 460 ms, zero layout shift |
| **Photograph weight** | The page draws its photographs at **406 to 980 px** on a laptop and **350 px** on a phone, measured in a browser, against files **1,200 to 1,800 px** wide. With an 800 px and an 1,100 px copy offered and the drawn width declared: **−62%** on a standard-density laptop, **−31%** on a retina one, **−41%** on a 3x phone, **−69%** on a 2x phone. First view untouched |
| **Nav bar width** | The eight links measure **873 px** rendered, against **833 px** available inside the 1,020 px column. **A synthetic off-DOM measurement of the same links said 793 px** — 10% out, and enough to be wrong about the answer |
| **Page word count** | About **5,900 words**. About was the longest section at **18.4%** before the cut |

---

## 5. What was got wrong along the way

1. **A distance nobody has recorded.** Rewriting the About lede, Claude wrote that the two of them
   were *"four hundred miles apart for most of the build"*. **Nothing in this repository records
   any distance.** Caught and removed the same minute, but it is exactly the §3d failure: filling a
   gap about physical reality by inference. The lede was deleted rather than replaced.

2. **Sizing the nav from a synthetic element.** To decide how many links fit, Claude measured a
   hidden `div` built off-DOM with the same declared font. It said **793 px**. The real rendered
   nodes measure **873 px** — 10% out, enough to be wrong about the answer. **Measure the real
   node.** The first fix shipped on that bad number and the bar wrapped to two rows.

3. **Fixing a breakpoint instead of a class.** The SVG labels were fixed at ≤760 px and the checker
   then found **40 labels at 9.1 px at 768 px** — the tablet band, which the media query did not
   reach. The real fix was to make the drawing's `min-width` equal its viewBox width at every
   width, and, for the hero chart which has to fit rather than scroll, a ladder computed so each
   band's narrow end still clears 12 px.

4. **A message sent to the wrong worker.** Figure findings went to the agent doing the documents,
   which **correctly refused, stayed inside its scope, and said so** rather than quietly widening
   its job. The findings were forwarded. It also found **a second plotted British spelling that the
   original message had missed** — `characterised` in fig09's subtitle, which renders into the
   image.

---

## 6. What was deliberately NOT done

- **`docs/`, `STATUS.md` and `CLAUDE.md` keep their British spelling.** The brief was the website.
  `check_facts.py` matches RETIRED qualifiers against real wording, so a blanket spelling change
  across the register and the protocol needs its own pass with that checker watched, not a sweep at
  the end of a long session. **The repository is therefore mixed: the deliverables and the site are
  US English, the operating documents are not.**
- **`sessions/` untouched.** History keeps its original numbers and its original spelling.
- **The `parts.json` base64 wrapper** (1.9 MB, 25% of which is the base64 padding) was left alone:
  GitHub Pages gzips, and the decoded payload compresses better than the base64 does, so the saving
  is largely absorbed. Worth revisiting only if the host stops compressing.

---

## 7. Open, for Jacob and Nuh

| | |
|---|---|
| ~~**How many rubber bands hold the sample plate?**~~ **ANSWERED, same session** | `SAID` Jacob, 2026-09-21: **three, each twisted three times — one across the screw pair by the scan head, two across the pair by the motor.** **Both earlier sources were right about what they saw and neither had the distribution**: the photo reading of the 2026-09-19 frames counted two, and `sessions/2026-09-17-bench.md` §17 recorded one more added at the bench. **The asymmetry IS the 2026-09-17 fix** — §149 had diagnosed that both bands pulled in line with the two side-by-side ball ends, the line the plate tips around, so the motor end lifted off its screw. Putting the extra band at the motor end moves the combined pull inside the triangle the three contacts make. **`docs/INVENTORY.md` now has a row, which it never had** |
| **Where did planning end and building begin?** | `docs/FACTS.md` has **11 weeks** and **8 weeks**, both `SAID`, with *"use both, and say which is which"*. **No boundary event is recorded anywhere.** The page had invented two different ones, and they were mutually exclusive — only three weeks separate first power-on from the last bench session |
| **Was a tip ever etched successfully?** | `docs/INVENTORY.md` still lists it as **UNKNOWN**, items (4) and (5). Three places on the page said or implied otherwise and no longer do |
| **Which op-amp is actually fitted?** | `docs/BOM.md` says OPA627AU, the Eagle source says OPA124U, **and nobody has read the marking on our own board.** The signal-chain diagram no longer names a part |
| **How tall is the instrument?** | The page says *"about a foot"* in three places. **No height is recorded in any file** — it is a by-eye figure from a photograph. Left as it is, because it is offered as a description rather than a measurement, but it is not a measurement |

## 7b. A correction from Jacob, mid-session

**`SAID` Jacob, 2026-09-21: *"We used etched tips the whole time."*** Asked to choose between
readings, he confirmed the tips were **sharp when fitted and blunted in use**.

**This answers two things `docs/INVENTORY.md` has carried as UNKNOWN since 2026-09-19** — item (4),
whether the etching setup was ever used successfully, and item (5), whether any fitted tip came off
it. Both yes. **`README.md`'s *"a cut tungsten tip, 0.25 mm wire cut at 45 degrees"* was simply
wrong** and is corrected; no tip in this project was ever cut.

**What it changes.** Tip bluntness moves from *an independent blocker* to *a consequence of the
snap*: the instrument drives the tip into the sample, and the last one fitted was pressed into the
gold four separate times in one morning, twice for over half an hour. **The fix is at the approach,
not at the tip** — a fresh tip only stays sharp if it stops being driven in. The website's blocker 3
was rewritten to say so, and it now points at blocker 1 rather than standing alone.

**What is still unknown, and still needs one sentence each:** the NaOH concentration, the voltage,
and the counter-electrode material. **Nobody can repeat the etch from this repository.**

**What looked unreconciled, and was resolved the same session by one more sentence from Jacob.**
The bench logs record his own words on two tips: *"horribly probably extremly blunt"* (2026-09-16)
and *"new tip its gonna be very blunt but that what we have for today"* (2026-09-19, ~03:00) — the
second said **before** that tip was used that day, which did not sit easily with "sharp when
fitted". **`SAID` Jacob, an hour later:** *"I attempted them, they were all hand-measured and
independent voltages, not good science, that's why they weren't so sharp — they changed sharpness
depending on run."*

**THE ETCH WAS NEVER A CONTROLLED PROCESS.** Each attempt was set up by hand, with the voltage
chosen fresh for that run and never written down. **So sharpness varied run to run and was not
known before the tip was fitted**, and both halves are true at once: a tip could arrive blunt from
a bad etch, and a tip that arrived sharp was then flattened by being pressed into the gold.

**The consequence is bigger than the blocker.** The tip is an **uncontrolled variable in every
result this project has**. Until one voltage, one concentration and one counter-electrode are
fixed and written down, **a failed run cannot be attributed to the tip or to the instrument** —
which is why the website's blocker 3 now says fix the etch first: it is the cheaper half, and it
is the one that makes the other half measurable.

**Item (2) of the etching row is now known to be unanswerable as a single value.** There is no
voltage to record, because there was never one voltage. That is a finding, not a gap in the notes.

**Process note.** This is the §3d case working as intended twice over: no amount of searching would
have found either half, because neither was ever written down. The first question changed a live
page, a blocker, two documents and the inventory. **The second — offered unprompted — turned a
flagged contradiction into a resolved one and made the blocker more useful than it had been all
day.** The right move on the first answer was to flag the conflict rather than smooth it over; if
it had been smoothed over, the second sentence would have had nothing to correct.

---

---

## 7c. Where it was left

**Jacob called it done for now**, 2026-09-21: *"I think it's rlly good this is the final version at
the moment."*

**Deployed and verified.** Eleven commits, `main` at `0b1bca3`, the Pages build and
`actions/deploy-pages@v4` both green. **The live page could NOT be fetched from the session
container** — the egress proxy refuses `jacobbkatz.github.io` with a 403 — so deployment is
confirmed from the Actions API and from `check_site.py` passing on the runner, not from reading the
served bytes. **Anyone re-checking this should open the page in a browser.**

**All four checkers green, each run as its own command:** `check_facts.py` 0, `check_layout.py` 0,
`check_site.py` 0, `audit_live.py` 0.

### Left undone on purpose, so nobody goes looking for it

| | |
|---|---|
| **`docs/`, `STATUS.md` and `CLAUDE.md` are still British spelling** | The deliverables and the website are US English; the operating documents are not. **`check_facts.py` matches RETIRED qualifiers against real wording**, so a blanket spelling change across the register and the protocol needs its own pass with that checker watched, not a sweep at the end of a session |
| **The two typefaces still load from Google Fonts** | The one render-blocking third-party request left on the critical path. Self-hosting two WOFF2 subsets removes it |
| **`model/parts.json` is 1.9 MB**, a quarter of it base64 padding | GitHub Pages gzips, and the decoded payload compresses better than the base64 does, so the saving is largely absorbed. Worth revisiting only if the host stops compressing |
| **Eleven of twelve sections are not named landmarks** | Only the price band carries an `aria-label`. Cosmetic for a screen reader |

---

## 8. Files changed

`deliverables/2026-09-19-pause/site/` (the page, `check_site.py`, `audit_live.py`,
`build_assets.py` — which now also writes two narrower copies of every photograph and stamps
`srcset` and a MEASURED `sizes` onto each tag — and `model/viewer.js`), `deliverables/2026-09-19-pause/figures/code/` (thirteen
figure scripts re-rendered), `deliverables/2026-09-19-pause/poster/` (US English, three stale
repository counts fixed, and a new `stamp_counts.py` that imports the website's own `repo_counts()`
so the two cannot drift), 31 deliverable documents, root `README.md`, and `docs/FACTS.md`, whose
**session-log row said 27 when there are 28** — it was counted before `sessions/2026-09-20.md`
existed, so the public page was right while the register was stale.
