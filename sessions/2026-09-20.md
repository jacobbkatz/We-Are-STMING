# Session 2026-09-20

**Nothing powered. Nothing touched. No measurement taken.** The instrument is disassembled and
moved. This was a deliverables session, run by Claude with Jacob directing, and its headline is a
correction to the project's central claim that Jacob himself argued for and won.

---

## 1. Headline

| | |
|---|---|
| **THE PROJECT'S CENTRAL CLAIM WAS WRONG, IN THE DIRECTION OF CAUTION** | Every deliverable said *"we have not demonstrated tunnelling"*. **That sentence conflates two claims and only the second fails.** Jacob's argument: a tip cannot travel from not-touching to touching without passing through the separations where tunnelling is the only mechanism carrying electrons, and bias was applied with the amplifier recording throughout. **The argument holds at every step.** `LEAD_VERIFICATION.md` **V13** |
| **THE CANONICAL SENTENCE IS NOW JACOB'S OWN** | *"We detected tunnelling, but weren't able to maintain tunnelling range for long enough to get an image."* Both halves separately sourced in `deliverables/2026-09-19-pause/FRAMING.md`. It supersedes every earlier formulation |
| **The loophole, and our own data closes it** | A metallic bridge shorting before the tunnelling separation would make the claim vacuous. **The I-V is superlinear (V^1.55) and symmetric; the junction never fell below about 5 MΩ even at amplifier saturation; 109 of 110 onsets were gradual ramps rather than steps.** A barrier, not a bridge — and a barrier conducting at 16-59 MΩ is conducting by tunnelling |
| **`d` IS BOUNDED, NOT RESOLVED** | **Under 0.5 mm** (`SAID`+`BENCH`, Jacob). **The 26 um a vacuum gap needs is INSIDE that bound**, in its bottom 5%, so it narrows the question without settling it. A straightedge and an eye cannot resolve 26 um; a loupe or USB microscope can. `docs/FACTS.md` |
| **THE 2026-09-19 READING OF `d` WAS WRONG TWICE AND BOTH ARE RECORDED** | *"Smack dab in the middle"* was written into three documents as *the centre of the piezo disc*, 1.000 mm. Jacob: *"No no I mean the tip is smack dab in the middle BETWEEN THE TWO SCREWS"* — centred **along** the pivot line, which says nothing about distance **from** it |
| **Project timeline settled** | **Eleven weeks of planning and design, eight weeks of building** (`SAID`). Four deliverables said eight and three said eleven; **both were right and none said which phase.** `docs/FACTS.md` gains a Project timeline section |
| **Repository confirmed PUBLIC**, and Nuh consented to the team photograph | Both `SAID`, Jacob, 2026-09-20. The consent is second-hand (Jacob relaying a text) and is recorded as such in all three permission records |
| **Poster page size confirmed** | 48 x 36 in landscape. It had been carried as PROVISIONAL since the poster was built, because no event specification exists anywhere in the project files |

---

## 2. State at a glance

| | Change today |
|---|---|
| Power | **Never switched on.** No LED check applies; no reading taken |
| Instrument | **Still disassembled and moved.** Untouched |
| Hardware | **No change of any kind** |
| Deliverables | **Rebuilt.** A new poster, four new figures, two new documents, and a correction pass across every deliverable |
| Publication | **All deliverables published to Google Drive as editable documents**, in Jacob's own account |

---

## 3. Measurements

**No new bench measurement.** Everything below is a re-derivation from raw files already in
`sessions/data/`, done independently of the bench scripts.

### 3.1 The hysteresis, re-derived from the raw CSVs

Recomputed from `bias_m01V_/m05V_/p01V_/p05V_1789822770.csv` without importing `ztest.py`, at the
geometric midpoint of the current range both branches share rather than the bench script's fixed
300-count level:

| Bias | Median loop | Counts per decade, going in |
|---|---|---|
| −0.1 V | **1,640** | 835 |
| −0.5 V | **714** | 1,505 |
| +0.1 V | **1,417** | 4,693 |
| +0.5 V | **1,926** | 1,328 |

**The session log records 705 to 1,868 across the runs; this independent pass gets 714 to 1,926.**
Two measurement rules, same answer. **Neither number replaces the other and both are now cited
together.**

### 3.2 Drift cannot explain the loop, and it had to be checked

**A gap closing during a cycle produces a loop of exactly the observed sign**, so this was the
alternative that had to be excluded before the hysteresis meant anything.

| Run | Median cycle | Drift (log §3.11) | Maximum drift contribution |
|---|---|---|---|
| −0.1 V | 3.2 s | +29 counts/s | **92 counts** |
| −0.5 V | 2.5 s | +83 counts/s | **207 counts** |
| +0.1 V | 6.1 s | +7 counts/s | **43 counts** |
| +0.5 V | 3.4 s | +22 counts/s | **75 counts** |

**At most 43 to 207 counts against loops of 714 to 1,926.** The log's own drift correction removes
at most about 120. **The loop survives either way.** The cycle durations are recomputed from the
timestamps in the files; the drift rates are the log's.

### 3.3 How long we spent inside the window where tunnelling had to appear

**This is the measurement that answers "could we have blinked and missed it".**

- The window: **20 ADC counts (about 62 pA, just above the 9-19 counts of noise) to 1,000 counts**,
  which is the Z test's own stop target — so every approach really traverses it. **1.70 decades, or
  about 0.17 nm of gap if it were a vacuum gap.**
- **Sampling: 227 readings per second, stepping Z 4 counts at a time.** Both recomputed from the
  raw files.
- **60,928 readings were taken inside that window, across 109 separate approaches** — a median of
  364 to 1,180 per approach.
- **A vacuum gap crosses that window in 10 to 22 Z counts.** The nearest we came was about 1,500.

**On the motor we are blind by construction**: one motor step moves the tip at least **1.94 nm** at
the top of `d`'s bound, which is **eleven times the whole window**. There is no motor position
inside it. **On the piezo we are not blind, and we looked.**

---

## 4. New faults

**None. No hardware was touched.**

---

## 5. Corrections to earlier documents

| Document | Correction |
|---|---|
| **`LEAD_VERIFICATION.md` V3, V10, V11, V12** | **Four mutually contradictory verdicts on the central claim were standing, with only one flagged.** All now carry forward pointers to V13. None deleted — the log is append-only |
| **`FRAMING.md`** | **It contradicted itself.** Its new opening made Jacob's sentence canonical while its closing section was still headed *"THE ONE CLAIM THAT CANNOT BE MADE — tunnelling current"*. Retracted in place, kept, with the corrected say-this/not-this table |
| **`docs/FACTS.md`** | `d` corrected from the withdrawn 1.000 mm to the bound. **Lever ratio, tip travel per motor step and the Z scale all become bounds** — over 80, under 1.94 nm, under 0.0078 nm/count. **Consequence: our own geometry now caps the Z scale BELOW the inherited 0.016 nm/count, so the two no longer agree** |
| **`docs/OPEN_QUESTIONS.md`** | The `d` question re-opened with the bound; both superseded readings kept |
| **`report/UPDATE_NOTE.md`** | The paragraph drafted for sending to an outside contact carried the wrong threshold (0.13 mm, which is a different criterion), the wrong range for `d`, and "thirty times" where it is 39. **All three fixed** — it is the text most likely to leave the project |
| **`manual/MANUAL.md` §12.3** | A row said the shields were *"metered end to end"* under a heading reading *do not spend bench time re-proving these*. **Four other documents say the check is still owed.** Row removed rather than edited, so it cannot read as "skip" |
| **`report/FINDINGS_REPORT.md`** | "its repeat half an hour later" corrected to minutes, four lines above the correction that had already been written |
| **`figures/code/fig02_iv_curve.py`** | Footer pre-dated V13 and read as a denial of tunnelling. Re-captioned |
| **`figures/code/fig13_*.py`** | **Docstring had drifted from its own code** — it still carried 61,038 readings, a 3.20-decade window and "four times the window", all of which the code had been corrected away from |

---

## 6. False trails

- **"The Drive upload mangled every table."** One agent reported blank header rows, literal `**`
  markers and backslash-escaped underscores. **It was an artefact of the tool reading the documents
  back, not a defect in them.** Confirmed by exporting a test document as HTML and finding real
  table cells with real `font-weight:700`. **An A/B of the markdown and HTML upload paths gave
  identical results.** Roughly an hour of planned re-upload work was avoided by checking.
- **Adding mass to engage the suspension.** Tempting, and `docs/NEXT_SESSION_PLAN.md` already says
  not to: the height adjusters are at maximum and 0.8-1.2 kg buys 20% on `f0` for travel that does
  not exist. **More importantly the gap motion is far too slow to be vibration at all** — a
  43,000-count excursion over 6.4 s is a one-way march, not an oscillation. **Suspension is the
  wrong physics for this blocker.**

---

## 7. Code and tool changes

| | |
|---|---|
| **`figures/code/fig10_push_pull.py`** | NEW. The 2026-09-17 step asymmetry: 23-28% of the predicted current change pushing in, 40-71% pulling out. **Scale-free.** Flagged on the figure as the one chart with no raw file behind it |
| **`figures/code/fig11_hysteresis.py`** | NEW. The in/out loops at all four biases, recomputed from raw CSVs, with the drift exclusion on the figure |
| **`figures/code/fig12_lever.py`** | NEW. What `d` would have to be. **Uses no inherited constant** |
| **`figures/code/fig13_could_we_have_missed_it.py`** | NEW. Answers Jacob's question directly |
| **`poster/render.py`** | Selector list updated to the rebuilt poster structure; page size note changed from provisional to confirmed |
| **`poster/poster.html`** | **Rebuilt.** Achievement-led panels, a new full-width panel 07 on the workflow, charts and photographs capped by height. 35.95 in of content on a 36 in sheet |

---

## 8. Next session

**Unchanged: the next session starts with REASSEMBLY.** `docs/NEXT_SESSION_PLAN.md` is the
procedure. **One item is promoted:**

> **Measure `d` with a loupe or a USB microscope before anything is powered.** It needs no
> electronics, it takes minutes, **it settles the 2026-09-17 junction retroactively**, and it fixes
> the nanometre scale for every measurement this instrument will ever make. A straightedge and an
> eye have already been tried and give only "under 0.5 mm", which is 19 times wider than the
> threshold.

**The five blockers, each with a one-line fix, are in
`deliverables/2026-09-19-pause/WHAT_HELD_US_BACK.md`.**

---

## 9. Process lessons

**Three of my own errors, all caught by someone else, all the same shape.**

1. **I bannered one superseded verdict and reported the class done.** Three more were standing
   unmarked. **`CLAUDE.md` §7.1 is written about exactly this** — a defect is a class, not an
   instance — and I did not grep. An upload agent found them.
2. **I reported a banner written that was never written.** The script that would have written it
   aborted on a later assertion before reaching its own `write`, and I reported the result without
   checking. **An `assert` after a successful edit discards that edit.** Verified by grep the
   second time.
3. **I corrected a figure's numbers in code and left its docstring saying the old ones.** Same
   class again, one file wide.

**And one that cost nothing because it was checked:** an agent reported the Drive tables were
mangled. **Believing that report would have meant an hour of unnecessary re-uploading.** Exporting
one test document settled it in two calls.

**What went right, and it was not mine.** Jacob argued the central claim was mis-stated, against a
confident and repeated position from me, and he was right. **That is twice in two days** — V10 was
the same shape. **The pattern is that I apply evidence from one junction, or one criterion, to a
different question, and state the result with more confidence than the reasoning carries.**

---

## 10. The website, second pass — and five defects only rendering could find

**Nothing powered, nothing touched.** This section is about the project website,
`deliverables/2026-09-19-pause/site/index.html`, published at
<https://claude.ai/artifact/YZ6sGyniLBBDNxhceXUfF8> (shared, anyone with the link).

Jacob asked for more photographs of the two of them, for a read-through of the page as a professor
or mentor receiving the link would read it, and for more animation.

### What was added

- **Five photographs of Jacob and Nuh**, captioned by name. **Every identification is Jacob's own**,
  from `Images/ours/README.md`; none was read off a frame (`CLAUDE.md` §7, the rule that cost five
  documents on 2026-09-09).
- **A section, "What we did not know when we started"** — eleven weeks of planning before the first
  board, neither of us having soldered, the etcher built rather than bought.
- **A contents list.** The page is **22,494 px tall at desktop width** and had no way to see what
  was in it or how far through you were.
- **A reading-progress hairline** at the top edge, for the same reason. It answers the reader's own
  scrolling, so it runs even under `prefers-reduced-motion`.
- **Three more pieces of motion**, each saying something the caption already says: the signal
  chain's four hops arrive in order and then the current returns along the gold path; the workflow
  counts count up. Both stop dead under reduced motion.
- **A reply path.** The closing block asked for help with the drift problem and gave no way to
  answer. It now points at the repository's issue tracker, which needs no address published.

### The five defects, and why none was visible in the source

**Every one of these was found by rendering the page in a browser and measuring it, not by reading
the HTML.** That is the transferable lesson.

1. **No image carried `width` or `height`.** Every photograph loads late, so until it arrived the
   browser left no room for it. **The page grew from 15,087 px to 22,494 px under the reader as it
   loaded** — half its length again, appearing under their eyes as they scrolled. `build_assets.py`
   now stamps each image's true pixel size onto its tag and the operation is idempotent.
2. **The three prepared photographs carried their caption burnt into a black bar** — and the page
   printed the same words again in grey underneath. Cropped with the poster's own `bar_top` rather
   than a second copy of it. **`build_assets.py` now fails if any built image still has a bar, and
   the check was run with the crop switched off to confirm it goes red**: it named all three
   (`CLAUDE.md` §7.2 — a test that has never failed has not been run).
3. **The Google Fonts URL was malformed.** One tuple carried two values where three axes were
   declared, so Google answered **400 and neither Newsreader nor Inter ever loaded**. The page had
   been rendering in Georgia and the system sans on every machine that opened it, including the
   published copy. Corrected; six faces now load.
4. **Two CSS selectors over-matched their descendants.** `.tally div` drew three rules per column
   where one was meant; `figure a` broke the poster's caption into seven lines, one per link. Both
   scoped with `>`. **Swept the whole page for the same class afterwards and found none left** — the
   §7.1 rule, applied this time.
5. **`about one nanoamp` was clipped to `nanoam`** by the signal chain's own viewBox, and `DSUB2`
   sat 40 px from the arrow it named.

### The same class again, in the figures

Checking the page's figures at full size found **`fig02` and `fig13` drawing their x-axis labels on
top of the first line of their own source footnote**, and `fig05` four pixels from doing it. All
thirteen were checked; those three were the only ones. Re-rendered with room, and the poster
re-rendered and still fits 48 x 36 in.

### Drive

The poster print file and the three re-rendered figures were **replaced in place on Drive**, so
every link already sent out still works. All four verified by MD5 against the repository:
`bbe54225…` for the poster, and the three figures match byte for byte.

### What this says about the process

**Nine of the eleven problems above could not have been found by reading.** The page passed a tag
balance check, an anchor check and an overflow check while the fonts were silently not loading and
the layout was growing by 7,400 px under the reader. **A page is not verified until it has been
rendered and measured**, the same way a number is not verified until it has been re-derived.

---

## 11. The website as the primary artefact: navigation, About us, and contact

**Nothing powered, nothing touched.** Jacob asked for three things: his email address on the page
instead of sending people to GitHub, an About us section, and a full design, content and
credibility pass with the changes implemented rather than proposed.

### Contact

**`jacob@quis.com` is the contact everywhere now** — the bar, the hero, the end of *What stopped
us*, the closing block and the footer. Jacob asked for it by name; the address used is the one this
session already had for him, and he can correct it in a word if it is the wrong one.

**GitHub is no longer a call to action.** One reference stays in the footer, where it backs the
claim that the record including the two withdrawn results is public — deleting it would have
removed the page's strongest credibility signal in order to follow an instruction that was about
where to send people, not about hiding the repository. **Nuh has no address on the page.** We do not
have one, and none was invented.

### Structure

The page now answers a first-time reader in order: **the claim and two actions open it**, above the
hero chart rather than in a block below it; *What the instrument is* comes first, so someone who
does not know what an STM is is not reading a personal story yet; the personal story moved into
**About us**, which is new and carries four real dates, five conditions that had been buried in the
footer, and the three photographs of Jacob and Nuh. The poster, the figure grid and the document
index were three thin sections and are now one. The eleven-item contents list is gone, replaced by
a sticky bar of six stops that marks the one the reader is in, and a footer that lists them all.

### Four defects, and one of them had stopped the page working

1. **A `mailto:` in the navigation panel was passed to `querySelector`, which throws.** That
   exception killed every observer below it — **the scroll reveals and the signal-chain animation
   had silently stopped running.** Found because a check that had printed
   `chain drawing drawn rv-in` for two days printed `chain`.
2. **Contrast.** `--muted` was **3.57:1** on this ground, under the 4.5:1 small text needs, and it
   sets every caption and figure source on the page. Now `#6A7278`, 4.77:1. The ranked numerals took
   the text gold rather than the mark gold, 2.34 to 4.77. **Zero failures at every size afterwards.**
3. **The hero chart's axis labels were inside the SVG**, so on a phone they scaled down with it to
   about four pixels. They are HTML now, and the gridline numbers and the trace are set larger below
   760 px so they come out the same size as on a laptop.
4. **Line length.** Captions, figure sources, document rows and the colophon ran to **88 to 178
   characters**. Every text block is now under about 86 at 768, 1280 and 1600 px.

### Two claims cut for want of a source

**What a commercial instrument costs**, and **what year of school we were in when we started.**
Neither is written down anywhere in this repository, and the About us section is the one place where
a reader would take them as fact about us.

### Checked before publishing

320, 390, 768, 1024, 1280 and 1600 px: no sideways scrolling anywhere, one `h1`, no heading-level
skips, every image has an `alt`, all ten in-page links land, the mobile menu opens and closes on
Escape and on choosing a link, no console errors, and the reduced-motion path leaves everything
visible and still.

---

## 12. The register recount, and three corrections from Jacob

**Nothing powered, nothing touched.**

### The recount

**Five documents and the website said `164 rows` in `docs/FACTS.md`**, dated, with the rule
*"table lines that are neither a separator nor a continuation"*. **That rule gives 167 on the same
file, and no rule anyone could write reproduced 164.** A number nobody can recompute is exactly
what the register exists to prevent, so the rule is now a program: `Code/pc/count_facts.py`.

It reports **157 — 137 live and 20 retired**. Updated in all six places, and `164 rows` is in the
RETIRED table so the checker finds any copy that survives.

> **The count does not live in `docs/FACTS.md`.** A row in the register that counts the register
> changes the answer every time anyone touches it — it went 156, 157, 158 while this was being
> written. It lives in the program that computes it, and documents quote it with the date they ran
> it.

**One weakness found and not yet fixed.** `check_facts.py` flagged only **one** of the six. Its
escape hatch — a line containing *retired*, *corrected* or *superseded* is treated as deliberately
quoting the old value — matched the unrelated phrase **"a RETIRED table"** in the other five. The
guard is weaker than it looks whenever the retired value's own row describes a table of retired
values.

### Three corrections from Jacob

1. **A 3D model of the instrument, in the page.** All 23 printed parts from `CAD/prints/`, 87,192
   triangles, plain WebGL2 with no library — both content delivery networks are blocked from this
   container, so a vendored library could not be fetched *or tested*. **The positions are an
   arrangement, not an assembly**, and the page says so: print files are laid out for a print bed
   and this repository does not hold the assembly transforms. `CAD/STM.f3z` turned out to be Mech
   Panda's upstream design in a proprietary format, not our assembled geometry.
2. **`metre` to `meter`** throughout the site, the poster and the scripts, with `behaviour`,
   `colour` and `centred`.
3. **The comments were cut back** to the ones that stop a future mistake — what breaks if the
   reveal script hides something above the fold, why a `mailto:` cannot go to `querySelector`, why
   the SVG type is set larger on a phone. The rationale for design choices is gone.

### What is still open

~~**Neither LinkedIn profile could be read**~~ **CLOSED the same day: Jacob sent screenshots of
both.** `linkedin.com` is blocked by the network policy here and neither profile is publicly
indexed, so the profiles are `SAID` — supplied by Jacob, 2026-09-20 — not read from the web.

**Nuh Shaheer:** mechanical engineering BE at WPI (2025–2029); undergraduate researcher in the Fang
Research Group on laser powder bed fusion and pore formation; AI and robotics software engineering
intern at ndimensions labs, building a ROS 2 model of a seven-axis arm from SolidWorks assemblies;
co-founder and program director of **Laces Up**, a fiscally sponsored non-profit, since 2023;
SOLIDWORKS and Fusion 360.

**Jacob Katz:** nanoengineering BS/MS at UC San Diego, with a study area in economics; NSF ELEAN
research intern at **Brookhaven National Laboratory's Center for Functional Nanomaterials**; runs a
trading-card grading and market-analysis business. **The page states plainly that none of the
Brookhaven work touched this instrument**, because the dates overlap and a reader would otherwise
be entitled to wonder.

**One figure deliberately left off the page:** the revenue Jacob's LinkedIn gives for the card
business. It is his to publish, not mine to surface, and it is not about the microscope.


---

## 13. The checker's escape hatch, fixed

**§12 recorded this and Jacob asked for it fixed rather than logged.**

`check_facts.py` exempted a **whole line** if any correction word — *retired*, *corrected*,
*superseded* — appeared anywhere on it, and it did that **before looking at which value matched**.
Retiring the register's row count caught **one stale copy out of six**: the other five sat on wide
table rows containing the phrase *"a RETIRED table"*, which names the mechanism and says nothing
about the number 200 characters further along the same row.

**Two changes, both in `Code/pc/check_facts.py`:**

1. **The excuse is now looked for near the value it excuses** — within 140 characters of where the
   retired literal actually sits, rather than anywhere on the line.
2. **An occurrence that is only naming the mechanism does not count.** *"a RETIRED table"*, *"the
   retired values"*, *"the superseded column"* are the name of a thing, not a statement that the
   number beside them is out of date.

**Proved both ways.** The exact line that slipped through was planted back into
`AI_WORKFLOW_SECTION.md`, phrasing and all: **the checker exits 1 and names it.** Restored, it
exits 0.

**It also found five lines nobody had looked at** — `sessions/README.md` summarises what each
session found, in that session's own terms, and five of those summaries quote a figure that has
since been retired. **The findings were not rewritten.** A marker sits beside each one saying it
has since been corrected, which is additive and is exactly what the checker's own message asks for.

---

## 14. The four wire bundles, and the figures made readable

**Nothing was powered and nothing was touched. No bench measurement was taken.**

### 14.1 Jacob named the four bundles, so they could be labelled

The scan-module photograph had been labelled for every part except the wiring. That was
deliberate: `docs/WIRING.md` section 11 says the four piezo quadrant wires are **bare enamelled
copper with no colour code**, and `CLAUDE.md` forbids reading wire colours off a photograph at all,
because the jumper leads at the scan head are different colours from the J1/J2 wiring. So the
question went back to Jacob rather than being guessed.

He sent four close-ups. **All four turned out to be crops of one frame we already had** —
`Images/ours/2026-09-19_platform_lifted_off_1.jpg`, IMG_4916, taken by Nuh at 10:15:40 on
2026-09-19 — which shows every one of the bundles at once. That frame is now
`deliverables/2026-09-19-pause/photos/prepared/16_scan_module_wiring.jpg`, with all four labelled,
and it is on the website.

Recorded as `docs/WIRING.md` section 12, with a warning across the top of it: **it is a routing
note, and no colour in it maps to a function or a pin.** It is for finding a bundle at the bench.
Never for deciding what to connect.

### 14.2 The figures were unreadable on a phone. Measured, not guessed.

Jacob asked for the figures to be made more readable. A render at a 390 px viewport put the
number on it: **fig03's footer came out at about 4 CSS px.** A grey smear. Three separate causes,
all fixed:

1. **The type was too small for the reduction.** The four sizes were 13.5 / 10.5 / 9.5 / 8.5 pt.
   They are now **17 / 13 / 12 / 11**.

2. **The footers were setting the width of every image.** `savefig` writes with
   `bbox_inches="tight"`, so the widest element on the figure decides how wide the exported PNG
   is — and that was always the footer, running a third wider than the chart itself. A browser
   then scaled the whole image down to fit its column, **shrinking the chart to pay for the
   footer's long lines.** `narrow_footer()` now re-wraps the footer to the width of everything
   else on the figure. **fig11 went from 3,550 px wide to 1,698 px for the same chart**; fig10 and
   fig12 roughly halved as well. Across the set, 12 pt text now lands at **12.3 to 17.5 CSS px**
   in the website's column, where it was 8 to 11.

3. **A phone cannot show a ten-inch chart, at any type size.** Below 760 px the website now keeps
   each chart at a readable 780 px and **scrolls it sideways inside its own frame**, with a line
   telling the reader they can swipe it or tap to open it full size.

### 14.3 What the bigger type broke, and the checker that found it

Raising the type broke **twelve of the thirteen figures at once** — footers straight through their
own x-axis labels, panel titles through the row below's tick labels, annotation blocks into each
other. Finding those by eye took four rounds of rendering and looking, which is exactly the kind
of thing this project keeps paying for twice.

**`deliverables/2026-09-19-pause/figures/code/check_layout.py`** now does it in one pass: it draws
every figure, asks the renderer for the bounding box of every piece of text, and reports any pair
that overlaps. **Proved red then green** — fig09's line height was set back to a deliberately
wrong value and the checker named six colliding pairs; restored, it passes.

Two things it got wrong first, both worth writing down:

- **It counted text that is never drawn.** matplotlib keeps a `Text` for every tick it has ever
  placed, including ticks outside the current view limits, and those still report a bounding box.
  Most of a run's findings were phantom ticks.
- **It counted empty space as ink.** Every line gets a box `fontsize × linespacing` tall, so a
  single 12 pt line at the house linespacing of 1.6 reports a box half again as tall as its
  glyphs. That made fig08's ladder rows look like collisions when they are seventeen pixels apart
  on the page.

`fit_footer()` also had to be rewritten. The first version made room by **compressing the axes**,
and on fig12 it squeezed a chart into a tenth of its height to fit a nine-line footer. Squeezing
the picture to fit the caption is the wrong way round. It now **adds paper at the bottom of the
figure** and leaves every axes the same physical size — which costs nothing, because a browser
scales an image by its width.

### 14.4 Two figures that needed rebuilding rather than nudging

- **fig07, the signal chain**, was six boxes in a row: twice as wide as it was tall, so a column
  scaled it to a third and a phone to a tenth. It is now **three across and two down**, with the
  chain wrapping round the end of the first row. Same six stages, half the width.
- **fig09, the timeline**, hard-coded its line height as `0.0315` axes units — correct for one
  type scale only. At the new scale every milestone headline landed on top of its own source line.
  The line height is now **derived from the type size**, so it cannot drift again.

### 14.5 Pushed to main

Jacob enabled GitHub Pages (Settings, Pages, Source: GitHub Actions) and asked for the work to go
to `main`. It did: `.github/workflows/pages.yml` builds the site and deploys it, so the page now
lives at **jacobbkatz.github.io/We-Are-STMING** with no dependency on anything else.

---

## 15. Four corrections Jacob caught in one sitting

### 15.1 A figure that argued against the page it sits on

Figure 13 was headed **"Could we have blinked and missed it? No. We took 60,928 readings inside
the window."** Jacob: *"this diagram is weird because it insinuates there was no tunnelling which
the previous header contradicts."* He is right, and the figure's own docstring had already said so
in capitals — *"THIS FIGURE IS NOT ABOUT WHETHER TUNNELLING OCCURRED"* — while the title said the
opposite to anyone who was not holding the whole argument in their head.

It is now **"Passing through the tunnelling window is not the same as parking in it"**, which is
what the evidence actually says and what the page says two screens above.

**And the caption was carrying a number nothing produced.** It said *"the nearest we came was
about 1,500 counts"*. Recomputed across all 109 approaches in all five files: the crossings run
**52 to 7,540 Z counts, median 2,108**. The two narrowest are real rather than a sampling
artefact — 14 and 12 readings inside the window at a 4-count step. The subtitle now takes those
five numbers straight from `crossing_stats()`, so they cannot drift from the data again.

### 15.2 The 3D model was missing the boxes, and this is the second time

Jacob: *"your missing all the boxes and sheild covers."* Correct, and the cause is worth recording
because it is a failure of reading rather than of arithmetic.

On the earlier pass he said *"you used mech pandas sheild cover not our custom one."* The model
was showing **`4_scanhead_box`**, which `CAD/prints/README.md` plainly calls the rejected
alternative — its corner ears are 142.8 mm and the shield cover's outside is 142.00 mm, so it does
not fit inside the thing it is meant to replace. **The fix was to swap it for `6_shield_cover`.
Instead all eight enclosures were deleted**, on the reasoning that they do not appear on the 3MF
print plates.

**A print plate is not an inventory** — the same mistake `CLAUDE.md` section 3d already names, in
a new costume. The photographs settle it: the copper-taped preamp box, the shield cover being held
in one hand, the controller in its box and the Teensy in its printed tray are all in
`Images/ours/`.

The model now carries **twenty parts in three groups**. `4_scanhead_box_base` and `_lid` are the
only printable files in this repository deliberately left out, and the script says why at the
point where it leaves them out.

Two viewer bugs surfaced with the extra parts, both from framing by a rule of thumb rather than by
measurement: the grid was laid out deeper than it was wide, so turning it three-quarters on swung
the far column off the left edge; and the camera distance was a fixed multiple of the radius,
which cannot be right for both a wide desktop stage and a phone's near-square one. It now frames
the **diagonal** of the footprint and computes the distance from the canvas's own aspect ratio.

### 15.3 The hero was half empty

Above 1000 px the claim, the buttons and the byline filled the left half and the right half sat
blank. The instrument goes in it — from `Images/ours/` rather than `photos/prepared/`, so it
arrives **without** the arrows and callouts. Labels belong on a figure a reader has already decided
to study, not on the first thing they see. Below 1000 px the photograph drops back under the text.

### 15.4 The price comparison was buried

It sat at the end of "What an STM is", four screens down. Jacob: *"move up the dollar comparison a
lot it's an attention getter."* It is now its own band **immediately below the hero**, headed *"A
commercial one starts at fifty thousand dollars."*

**The caveat moved with it and was not weakened.** The $64 is the controller board alone — the one
cost in this project with an order record behind it — and the three commercial figures are the only
numbers on the page that were not measured here. Both statements sit in the same band as the
numbers they qualify.

### 15.5 The artifact points at the real site, and the first attempt broke it

The claude.ai artifact is now a signpost to **jacobbkatz.github.io/We-Are-STMING**.

**The first version of it showed Jacob a blank grey page reading "This content is blocked."** It
carried `<meta http-equiv="refresh">` and a script calling `window.top.location.replace`, on the
theory that a visible link was the fallback if either were refused. Both were refused, and there
was no fallback, because the refusal replaces the whole frame rather than letting the page render.

**Reproduced in a local iframe harness with the same sandbox flags** before changing anything:
Chrome reports *"The frame attempting navigation of the top-level window is sandboxed, but the flag
of 'allow-top-navigation' ... is not set"*, and the frame ends on an error page. With both the meta
refresh and the script taken out, the card renders in that same harness, in light and in dark.

So there is no redirect and there cannot be one: **the link is the mechanism**, and the page says
so in one line rather than pretending. The reason is written into the top of
`deliverables/2026-09-19-pause/artifact_redirect.html` so nobody puts the meta refresh back.

---

## 16. One audit of the whole website

Jacob asked for a full audit. It is written up here in the order the findings matter, not
the order they were found. **Two programs came out of it, both in
`deliverables/2026-09-19-pause/site/`:** `check_site.py` reads the markup, `audit_live.py`
loads the page in a real browser at five widths in both themes. Both run in the deploy now,
and `check_site.py` fails the build.

### 16.1 The page had no `<head>`, and that broke every phone

**`index.html` began at `<title>`.** No `<!doctype html>`, no `<html lang>`, no
`<meta charset>`, no `<meta name="viewport">`, no `<body>`. Four consequences, all measured
rather than reasoned about:

| | Before | After |
|---|---|---|
| `document.compatMode` | `BackCompat` &mdash; **quirks mode** | `CSS1Compat` |
| Layout width on a 390 px phone | **980 px**, scaled down to a third | 390 px |
| `(max-width:760px)` matches | **false** | true |
| Character encoding | guessed **windows-1252** | UTF-8 |

**Every mobile rule on this site was dead on a real phone.** The swipeable charts, the
single-column hero, the nav menu, the figure sizing &mdash; none of it fired. The page was
laid out at desktop width and shrunk.

**And every test I had run missed it**, because Playwright's plain `viewport=` sets the
layout width directly. A handset does not: it reads the viewport meta and falls back to
980 px without one. `audit_live.py` now emulates a real device (`is_mobile`), which is the
only reason this is written in the past tense.

### 16.2 Three numbers about the repository were stale on the live site

The page states seven counts about this repository. Three were wrong:

| | Page said | Actually |
|---|---|---|
| `check_facts.py` | 565 lines | **598** |
| Session logs | 27 | **28** |
| Commits | 106 | **116** |

`CLAUDE.md` 575 lines, `docs/FACTS.md` 157 rows, 163 raw data files and 92 photographs were
all correct.

**Typing them is the bug.** Each is now wrapped in `data-repo-count="..."` and
`build_assets.py` measures it and stamps it at build time, so the deployed page cannot state
a count the repository does not have. `check_site.py` also checks them, for a page committed
without a rebuild. Proved red then green.

> **A trap inside the fix.** The counter runs `git rev-list --count HEAD`, and
> `actions/checkout` clones shallow by default &mdash; so the first deploy would have stamped
> **"1 commit"** onto the live page. `fetch-depth: 0` is now set, with the reason written
> beside it.

### 16.3 The hero chart could not be recomputed

The page says, four lines above it, *"every measurement on this page can be recomputed from
the raw files"*. The hero trace was the exception: its points were written into the markup
and **no script in this repository reproduced them.** `build_hero_trace.py` now draws it from
`sessions/data/2026-09-19-morning/ztest_1789822585.csv`, choosing the approach whose reading
count is closest to the median &mdash; the same rule figure 13 uses &mdash; and the caption
names the file and the cycle. The span it reports changed from 2,640 counts to **1,920**,
which is what that approach actually is.

### 16.4 The model note contradicted the page two blocks above it

Under the 3D model: *"These twelve are the parts on our own print plates &hellip; we did not
print them: on our instrument the shielding is copper tape, and the boxes are not there at
all."* The paragraph above it says twenty parts including the boxes and the shield cover, and
the photographs show them. **This was my own leftover from the wrong correction in &sect;15.2**
and it survived that fix. Rewritten.

### 16.5 The loosest claim on the page

*"Two second-year engineering students who wanted to see single atoms, and built the
instrument that can."* Read plainly, that says their instrument resolves a single atom. **It
has not.** Both occurrences now say they built one of the few instruments that *can* resolve
one, which is what was meant and is true of the design rather than of this build.

### 16.6 A promise that was not ours to make

The footer said *"This page sets no cookies and collects nothing about you."* The page loads
two typefaces from Google Fonts. No cookie, but a third party does see the reader's address.
The sentence now says exactly that.

### 16.7 An arithmetic slip repeated from the plan

The page said a scan covered *"about 0.6 nanometers of sample &mdash; smaller than a single
atom"*, from `docs/NEXT_SESSION_PLAN.md` and `sessions/2026-09-17-bench.md` &sect;3.25.
**&plusmn;0.6 nm is a span of 1.2 nm, and gold's atoms sit about 0.29 nm apart &mdash; four
atoms, not less than one.** The conclusion is unchanged: four atoms is still far too small a
field to show a surface. **The session log keeps its wording, because a log is history.** The
correction is in the live plan, and the page now says "a few atoms across" and points out
that the figure rests on the scale row 4 says was never established.

### 16.8 Two megabytes nobody had asked for

`model/parts.json` is 1.9 MB and was fetched on every page load, for a model a fifth of the
way down a page 40,000 px tall. It now loads when the reader comes within a screen of it.
**First view fell from 2.31 MB to 0.29 MB**, measured, and the model still works.

### 16.9 Smaller things

- Touch targets: 36 px clears WCAG's 24 px but is tight with a thumb. On a coarse pointer
  every control is now 44 px or more.
- `viewer.js` is `defer`red.
- `drive/README.md` opened with *"The twenty documents"* and has 22 rows. Corrected &mdash;
  and **the 22 Drive document IDs existed nowhere but `index.html`**, so losing that one file
  would have lost the link between every document and its source. They are recorded in the
  manifest now.

### 16.10 What came back clean

Anchors and ids, local file references, image dimensions, alt text, heading order, WCAG AA
contrast on every token pair in both themes, duplicate paragraphs, sideways scroll at five
widths, console errors, failed requests, and every other number on the page against
`docs/FACTS.md` and the session logs.
