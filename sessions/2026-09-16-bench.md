# Session 2026-09-16 bench

**Who was there:** Jacob, with Claude Code desktop running **on Jacob's Windows machine**, so this
session can reach the Teensy's USB port.
**Type:** setup, then the first controller power-up. **The bench part was still under way when this
log was first committed.**
**Goal for the session:** finish the first-time setup on Jacob's machine, prove the tools with the
no-Teensy dry run, then work the list at the top of `docs/NEXT_SESSION_PLAN.md`.

> **A different session from [`2026-09-16.md`](2026-09-16.md)**, which was Jacob with Claude on the
> web earlier the same day. **Neither supersedes the other.** That one found that a web session
> cannot reach the Teensy and stopped; this one is the restart on the local machine that it asked
> for.

---

## 1. Headline

| | |
|---|---|
| **Jacob's Windows machine works** | Repository cloned, pyserial installed, and the dry run gave the pass: `No Teensy found. Ports seen:` with nothing plugged in. `check_facts.py` clean |
| **On that machine, type `py`, never `python`** | `python` and `python3` are **Microsoft Store placeholders** that print "Python was not found" although Python 3.13.15 is installed. The `py` launcher works |
| **The commit guard would have blocked every commit on this machine** | `.githooks/pre-commit` and `.claude/session-start.sh` both called `python3` blindly. **Fixed, and the guard proved in both directions**: it passes a clean tree, blocks a planted fault, and blocks when no Python exists |
| **The web session's changes are already on `main`** | `STATUS.md` and `sessions/README.md` said they were only on a branch. **Nothing needs merging** |
| **First power-on: one supply channel went into current limit at about 2 V. Switched off** | **One channel's limit was 20 mA and the other's 200 mA**; the 20 mA setting was left over from the preamp tests, while the controller draws **60 mA and 36 mA** (measured 2026-08-29). That explains it. **The supply wiring is still to be proved at the plug before the limit goes to 200 mA.** §3.1a |
| **THE ADC CHAIN WORKS** | At 200 mA limits the controller ran at about 40 mA with no current limit. USB needed its plug fully seated. After `RSET`, LEDs dark, Z parked: **20 `ADCR` readings average −1.7 counts, no drift**, matching last night's −0.4 mV meter reading. **The first ADC reading in this project inside the converter's input conditions.** The spread is not a noise figure: shield cover off. §3.2b |
| **"The controller's draw was never recorded" was wrong** | Both the plan and the earlier 2026-09-16 log said so. **It was measured on 2026-08-29** and sits in the handoff's history section |

---

## 2. State at a glance

| Stage | Status | Change today |
|---|---|---|
| Jacob's Windows machine | **working** | New to the project this morning; now proved with the dry run |
| Controller with the preamp attached | **power-up under way** | Supplies being switched on. Nothing measured yet |

---

## 3. Measurements

### 3.1 The bench — nothing recorded yet

**`SAID` 2026-09-16, Jacob, about 14:00 UTC: "the stm tip holder still isn't soldered to the
standoff, turning on the supplies now."** So **there is no tip lead**, which is the configuration the
plan asks for. **The rest of the plan's configuration** (no tip, no sample plate, no overall shield
cover, bench supply off the preamp) **was not restated this session and is not confirmed here.**

### 3.1a First power-on attempt — ONE CHANNEL WENT INTO CURRENT LIMIT. Switched off

**`SAID` 2026-09-16, Jacob, shortly after 14:00 UTC:** both supplies set to 18 V, switched off,
connected to the board, switched back on. **One channel went into constant-current mode with its
voltage at about 2 V; the other held 18 V.** Claude told Jacob to switch both off, which is also
what the plan's step 3 says.

**`SAID` a few minutes later, Jacob: "yes they are at 20mA but dont we normally run at 20mA".**
**The limits were 20 mA.** Answered: 20 mA was the preamp-alone setting, for a board drawing about
4 mA per rail; **the controller has never been run at 20 mA**, and its 2026-08-29 bring-up used
200 mA.

**Refined minutes later, `SAID`, Jacob: "one is at 20mA one is at 200mA".** **The limits were NOT
equal: one channel at 20 mA, the other at 200 mA.** That explains the event more directly than the
rail-to-rail argument below: **the 20 mA channel limited and the 200 mA channel did not.** Jacob set
both to 200 mA. Which channel had the 20 mA limit was not stated; both of the controller's measured
draws exceed 20 mA, so either would have limited.

**Still not reported:** **which channel** went into current mode, and how the two channels are
joined.

**Leading explanation, now SUPPORTED by the limit setting but with the wiring still unproven: the
limits were still at 20 mA from the preamp tests.** Every
preamp run on 2026-09-15 used a 20 mA limit. **The controller's own draw was measured at the first
power-up on 2026-08-29: V++ 60 mA, V-- 36 mA at ±18 V** (`docs/PROJECT_HANDOFF_SUMMARY.md` §A.1.11).
Both exceed 20 mA. **Why only one channel limits** is consistent with this: V++ feeds three
regulators and every digital rail, so it hits the limit first and collapses; most of the V-- load is
op-amp and DAC supply current that flows rail to rail, and it falls away once the positive rail is
starved, which can leave the negative channel below its limit and at 18 V.

**The other live explanation is a wiring fault at the supply**: a channel reversed, or the two
channels not stacked in series with their junction on AGND. **That is why the instruction given was
to prove the wiring before raising the limit**, not after:

1. Plug out of U19. Both limits to about **200 mA**, the value the 2026-08-29 bring-up used.
2. Outputs on, plug hanging free. Black probe on the plug's middle wire; the outer wires must read
   about **+18 V and −18 V**.
3. Outputs off. On the board, beep each outer U19 pin to **U18's middle leg**, pad 2, the V-- input
   (`docs/WIRING.md` §7). **That pin must receive the −18 V wire.**
4. Only then plug in and power. **Expect roughly 60 mA and 36 mA**, plus a few mA for the preamp.
   **A channel still in current mode at 200 mA is a real fault: switch off and do not retry.**

**Damage assessment, reasoned rather than measured:** with a limit that low, very little power
reached the board. Running briefly with the negative rail present and the positive rail starved has
**not been checked chip by chip** against every datasheet.

### 3.1b Second power-on, both limits at 200 mA — WORKS

**`SAID` 2026-09-16, Jacob: "Just powered on working as expect current draw is around 0.04A".**
**No current limit this time.** About **40 mA**, but **which channel that is, or whether both read
it, was not stated**. Against the 2026-08-29 baseline plus a few mA for the preamp, that is about
64 mA on V++ and 40 mA on V--, so **40 mA matches the negative channel closely**. If it is the
positive channel, it is lower than that baseline, which is not in itself a fault: the conditions of
the 2026-08-29 reading are not fully recorded. **Whether the plug-voltage check was done before this
power-on was not stated.** USB not yet connected: a port scan from this computer found no serial
ports at all.

**`SAID`, Jacob, next message: "The leds are on and current isnt climbing, just plugged in the
usb".** **The negative-channel watch passed**, with LED1–LED4 lit as expected at power-on.

**USB: THE COMPUTER DID NOT SEE THE TEENSY.** `stm_console.py GSTS` returned `No Teensy found. Ports
seen:` with an empty list. Windows' own device list, checked by Claude, showed **no device with
PJRC's vendor ID 16C0, present or ever seen on this computer, and no failed or unrecognised USB
device either.** So this is **not a driver problem**: nothing attempted to connect. **Leading causes,
none yet tested:** a charge-only USB cable, which `Code/pc/README.md` already names as the usual
cause; the plug not fully seated at the Teensy; or a hub or dock port.

**`SAID`, Jacob: "just plugged it in fully".** **The plug had not been fully seated.** On the retry
Windows enumerated the Teensy as **COM3**, VID 16C0 PID 0483, the normal serial firmware and not the
bootloader. **Same cable, so the charge-only-cable cause is ruled out.**

### 3.2a The serial link, and RSET — sent by Claude from this computer

| UTC | Sent | Reply |
|---|---|---|
| ~14:19 | `GSTS` | `0,0,0,0,290,0,0,0,0,27193`. Uptime 27.2 s, so the Teensy booted when USB went in. **DAC fields are boot placeholders. The raw ADC value of 290 is VOID**: LED1–LED4 were lit and the DACs unconfigured |
| ~14:19 | `RSET` | silent, as documented |
| ~14:19 | `DACZ 32768` | silent, as documented |
| 14:19:47 | `GSTS` | `0,32768,0,0,80,0,0,0,0,62462`. **Z commanded to 32768, 0 V.** Bias, X and Y at 0, zero scale, left there by `RSET`. Raw ADC **80 counts**, taken within a second of the park and **before anyone confirmed the LEDs had cleared**, so not a result |

**Two-minute settle counts from 14:19:47 UTC.** **`SAID`, Jacob, during the wait: "dac leds are
off".** LED1–LED4 dark after `RSET`, which is the pass.

### 3.2b THE ADC CHAIN WORKS. Mean −1.7 counts, no drift

**Configuration:** preamp boxed and spliced to DSUB2, no tip, no tip lead, no sample plate, overall
shield cover off per the plan (not individually re-confirmed this session). Z at 32768. Hands away
from the box and cable, as asked. **LED1–LED4 dark before the readings; not re-checked after.**

**The plan's three readings and control**, 2 min 5 s after the park:

| UTC | Command | Counts |
|---|---|---|
| 14:21:52 | `ADCR` | **89** |
| 14:21:53 | `ADCR` | **22** |
| 14:21:55 | `ADCR` | **−13** |
| 14:21:56 | `GSTS` field 5, raw | **−94** |

**Those four fell in a row**, so 20 more `ADCR` were taken straight away, 14:22:33 to 14:23:02, to
tell drift from noise:

`12, −59, 25, 33, −14, −34, 4, −39, 7, −69, 44, −29, −38, 89, 71, 51, −66, 12, 0, −34`

| | Counts | At 0.3125 mV per count |
|---|---|---|
| **Mean of 20** | **−1.7** | **−0.5 mV** |
| Standard deviation | 45.0 | 14 mV, about 0.14 nA through 100 MΩ |
| Range | −69 to +89 | |
| First ten / last ten | −13.4 / +10.0 | **no drift** |
| `GSTS` raw at 14:23:02 | −231 | a single conversion, not averaged |

**Result: the mean sits within two counts of zero, which is "the chain works" in the plan's table.**
**It agrees with the meter:** last night's −0.4 mV on the OUT lead predicts about −1.3 counts, and
the mean is −1.7. **Every ADC reading before this was taken outside the converter's input
conditions**; this is the first inside them. For scale, the old board's baselines were around −400
and near 29,500 counts.

**The four falling readings were noise, not drift**: the next twenty wander both ways and the two
halves' means straddle zero.

**What this does NOT establish:**

- **The spread is NOT a noise figure.** The overall shield cover is off, and the plan says noise in
  this configuration is not representative. **Do not record 45 counts as the instrument's noise.**
- **A near-zero mean is also what an ADC input shorted to ground would read.** The splice was
  verified by continuity on 2026-09-15, and a 45-count spread suggests a live input rather than a
  dead short, **but the converter's own noise floor is not in this repository to compare against**,
  so that argument is not settled. **A known signal through the preamp would prove the chain end to
  end**; none was applied. The tip lead's re-measure is the natural next chance.
- **The raw single conversions (−94, −231) spread wider than the averages**, as a 5-sample average
  should. Not interpreted further.

### 3.2c The tip lead is built. Safety rule 7's meter check passes

**`SAID` 2026-09-16, Jacob, after the ADC result: "All set the standoff is wired to the tip and
there is continuety between the two, no continuiety between brass of peizo and tip".**

**Read as:** the tip lead now joins the preamp's standoff to the **tip holder**, and **no tip is
fitted**. Stated back to Jacob for correction; **not yet confirmed.** Power-down before soldering was
advised; **whether it was done was not stated.**

| Meter check, unpowered | Result | Meaning |
|---|---|---|
| Standoff to tip holder | **continuity** | The joint is made at both ends, so **the enamel was burned through at both ends**. An open joint here would rail the output and look like a catastrophic leak |
| Piezo brass electrode to tip holder | **no continuity** | **Safety rule 7 passes.** The brass is grounded, per Berard (`docs/UPSTREAM_BERARD.md`); a bridge would be a shunt across the preamp input |

**What the second check does NOT establish:** a multimeter's open reading proves only more than
about 60 MΩ, and a path in the 100 MΩ to 10 GΩ range would still matter here (safety rule 12). **The
ADC re-measure is the test that reaches that range.** The tip holder was **not** metered against the
scan electrodes on the ceramic face; the brass plate and the insulating disk sit between them.

**Not stated, and asked:** whether the joint was cleaned with 99% IPA; how the standoff is now held
against the lead's pull; whether the box is fixed in its final position; that the lead is slack.

**Next: re-measure through the ADC** with the same power-up sequence, **against tonight's −1.7-count
baseline.** Any shift belongs to the lead, the holder or the piezo.

**`SAID`, Jacob, answering: "No tip fitted, supplies on, usb plugged in, cleaned with IPA,wire is
slack".** **All four open points confirmed.** How the standoff is held and the box's final position
were not restated. Supply currents not reported this time.

### 3.2d Re-measure with the tip lead on — THE MEAN DID NOT MOVE MEASURABLY. The spread tripled

Same sequence, commands sent by Claude:

| UTC | Sent | Result |
|---|---|---|
| ~14:58 | `GSTS` | `0,0,0,0,7,0,0,0,0,14980`, uptime 15.0 s. Link good first time. **Raw 7 is VOID**, LEDs lit, but **not pinned, so the joint was not open** |
| ~14:59 | `RSET`, `DACZ 32768` | silent |
| 14:59:07 | `GSTS` | `0,32768,0,0,-16,...`. Z at 32768 |
| — | — | **`SAID`, Jacob: "leds are off"** |
| 15:01:12 to 15:01:15 | `ADCR` ×3 | **−46, −40, −63** |
| 15:01:16 | `GSTS` raw | **−28** |
| 15:01:18 to 15:01:45 | `ADCR` ×20 | `-43, -105, 73, -325, 94, -169, -54, -103, -40, 184, -160, -66, -102, 153, 109, 121, -46, -50, -81, 164` |
| ~15:01:47 | `GSTS` raw | **−260** |

| 20 `ADCR` | Before the tip lead, §3.2b | **With the tip lead** |
|---|---|---|
| **Mean** | −1.7 counts | **−22.3 counts** |
| **Standard deviation** | 45.0 | **131.5** |
| Range | −69 to +89 | **−325 to +184** |
| First ten / last ten | −13.4 / +10.0 | −48.8 / +4.2, **no drift** |

**The mean moved by −20.6 counts, about −6 mV, or −64 pA through 100 MΩ.** **That is not a
measurable shift at this sample size**: the standard error of the difference between the two means
is about 31 counts, so the change is under one standard error. **Any leakage the lead, holder and
piezo added is below roughly 60 counts, about 0.2 nA, at two standard errors.** **Under the plan's
table this is the "within a few tens of counts" row: the tip lead passes.** The resolution is set by
the wider spread, **not** by the ~4 pA the meter reached on the bare board.

**The spread roughly tripled, 45 to 131.5 counts, about 0.4 nA.** **It is still NOT a noise
figure**, because the overall shield cover is off. **Mechanisms that could produce it, none yet
tested:**

1. **Pickup on a much larger input node** — tip holder, 40 AWG lead and the piezo assembly are now
   part of it, uncovered. The most likely, and the cover is the test.
2. **Coupling from the scan electrodes** through the disc. Their DACs are static, so this would be
   their noise rather than a signal.
3. **The slack lead moving**: vibration or air on a high-impedance wire makes charge.
4. **Someone within a metre** (safety rule 9). Jacob was asked to keep away; distance not recorded.

**For scale: 0.4 nA of scatter against a 1 nA tunnelling current is too much to image through.**
**Fitting the cover and repeating these twenty readings is the discriminator**, and it is the first
representative noise figure this instrument would have.

### 3.2e Shield cover on — THE SPREAD GOT WORSE, NOT BETTER

**`SAID`, Jacob: "ok all set big sheild is on and both sheilds are gounded to universal ground go
for it".** **"Universal ground" is not defined anywhere in this repository**, and
`docs/OPEN_QUESTIONS.md` says this design has no universal or chassis ground. **Claude's reading,
stated to Jacob before sending anything: the building's mains earth**, such as a supply's earth
post. **NOT CONFIRMED — asked.** Whether the supply was switched off to fit the cover was not
stated.

| UTC | Sent | Result |
|---|---|---|
| 15:12:16 | `GSTS` | `0,32768,0,0,72,0,0,0,0,818566`. **Uptime 818.6 s: USB never unplugged.** Z shows 32768, but that is bookkeeping; if the supply was cycled the DACs restarted unconfigured, so the reset was repeated |
| ~15:12 | `RSET`, `DACZ 32768` | silent |
| 15:12:34 | `GSTS` | `0,32768,0,0,9,...` |
| — | — | **`SAID`, Jacob: "leds when dark"**, read as the LEDs went dark |
| 15:14:39 to 15:14:42 | `ADCR` ×3 | **241, −83, 109** |
| 15:14:43 | `GSTS` raw | **−587** |
| 15:14:45 to 15:15:12 | `ADCR` ×20 | `381, 476, -356, 427, -483, -130, 11, 77, 162, 187, 79, 449, -125, -350, 68, -4, 408, 110, 332, -333` |
| ~15:15:14 | `GSTS` raw | **−784** |

| 20 `ADCR` | No tip lead, no cover | Tip lead, no cover | **Tip lead, cover on, shields to "universal ground"** |
|---|---|---|---|
| **Mean** | −1.7 | −22.3 | **+69.3** |
| **Standard deviation** | 45.0 | 131.5 | **294.9**, about 0.9 nA |
| Range | −69 to +89 | −325 to +184 | **−483 to +476** |
| First ten / last ten | −13.4 / +10.0 | −48.8 / +4.2 | 75.2 / 63.4, no drift |

**The mean change is not significant**: +91.6 counts against a standard error of the difference of
about 72. **The spread more than doubled.** The raw single conversions, −587 and −784, are the
largest this session.

**The shape is suggestive, not proof.** Half the twenty readings sit beyond ±300 counts, bunched
near two extremes of about ±480. **A steady sinusoidal hum sampled at random moments bunches at its
peaks like that**, where random noise would cluster near the middle: with this spread, random noise
predicts about six of twenty beyond ±300, and a sine of ±480 predicts about eleven. **Twenty
readings cannot settle it.**

**Mechanisms, none yet tested:**

1. **The shields are bonded to a ground that is not the circuit's reference.** A shield at earth
   while `AGND` sits somewhere else couples the difference — typically mains hum — **straight onto
   the input node** instead of blocking it. **This is why the project's rule is one bond per shield
   to circuit ground, `AGND`** (`docs/ENGINEERING_REFERENCE.md` §3). **Fits the result best if
   "universal ground" is mains earth.**
2. **A ground loop**: if `AGND` is also earthed some other way, such as through the laptop's USB
   ground and its charger, the shield bond closes a loop.
3. **Fitting the cover disturbed the scan head** — the lead, the holder, or something touching.
4. **Operator distance.** Jacob was watching the controller's LEDs during the wait; how close
   to the scan head was not recorded (safety rule 9).

**Next discriminator: move each shield's single bond to `AGND`**, with nothing else changed, and
repeat the twenty readings with Jacob at least a metre away.

### 3.2f Paused — about 15:17 UTC

**`SAID`, Jacob: "Ok Ima pause for a bit powering off".** **The instrument is being powered off; the
bench is paused, not finished.** Left as it stands: tip lead on, no tip, cover on, both shields
bonded to "universal ground". **What "universal ground" is was still unanswered at the pause.**
Power-off order advised: USB out, then supply off. **Whether it was followed was not stated.**

### 3.2g Resumed about 18:10 UTC. "Universal ground" IS `AGND` — §3.2e's leading mechanism is WRONG

**`SAID`, Jacob: "uni versal ground is the junction between the two power supply, the sheilds dont
touch other, uni ground is what 2 is".** **So both shields are bonded to the junction of the two
supply channels, which is `AGND` at U19's middle pin — circuit ground, exactly what the project's
rule asks for.** **Claude's reading of "universal ground" as mains earth was wrong**, and **mechanism
1 in §3.2e is ruled out.** Mechanism 2, a ground loop through that bond, is not ruled out by this
alone. The two shields do not touch each other.

**Jacob's own hypothesis, recorded as asked:** *"could it be that now that all the wires have to come
out of the same hole in the sheild that could cause addtional induced currents"* — cables passing
through one opening in the cover. **A real effect in principle**, since conductors entering a shield
can carry outside noise in with them. **Not tested separately**; see the result below.

**Also `SAID` on resuming:** powered up, USB in. **"dac lights are off" before any `RSET` was sent** —
**this contradicts every earlier power-on tonight**, when LED1–LED4 were lit until `RSET`. The order
of supply and USB this time was not stated; the Teensy's uptime at the first `GSTS` was 61.6 s.
**Recorded, not explained.** Supply currents: **"steady at 0.56 and 0.07 A"**, then corrected by
Jacob to **0.056 A**. **Read as 0.056 A and 0.07 A. Which is positive and which negative was not
stated.** Both above the 2026-08-29 figures of 60 mA and 36 mA plus a few mA for the preamp, by an
amount **not explained**; steady, and no current limit. **Whether the soldering iron or other bench
equipment was on during §3.2d and §3.2e, or is off now, was asked and NOT answered.**

### 3.2h SAME CONFIGURATION, OPERATOR A METRE AWAY: STANDARD DEVIATION 12.7 COUNTS

**Nothing changed in the hardware since §3.2e**: tip lead on, no tip, cover on, both shields on
`AGND`. **What did change:** Jacob was asked to stand at least a metre from the scan head for the whole
capture; a break of about three hours; a full power cycle; and possibly the state of other equipment
in the room.

| UTC | Sent | Result |
|---|---|---|
| 18:15:00 | `GSTS` | `0,0,0,0,72,0,0,0,0,61594`. Fresh boot, link good |
| ~18:16 | `RSET`, `DACZ 32768` | silent |
| 18:16:41 | `GSTS` | `0,32768,0,0,45,...` |
| — | — | **`SAID`, Jacob: "still dark"** |
| 18:18:56 to 18:18:59 | `ADCR` ×3 | **10, −10, −2** |
| 18:19:01 | `GSTS` raw | **11** |
| 18:19:02 to 18:19:29 | `ADCR` ×20 | `19, -14, 0, -30, 3, -4, 4, -9, -16, -24, -3, -4, -8, 18, 3, -10, 3, -9, 14, 7` |
| ~18:19:31 | `GSTS` raw | **117** |

| 20 `ADCR` | §3.2b no lead, no cover | §3.2d lead, no cover | §3.2e lead, cover | **§3.2h lead, cover, operator away** |
|---|---|---|---|---|
| **Mean** | −1.7 | −22.3 | +69.3 | **−3.0**, about −9 pA |
| **Standard deviation** | 45.0 | 131.5 | 294.9 | **12.7**, about 40 pA |
| Range | −69 to +89 | −325 to +184 | −483 to +476 | **−30 to +19** |
| Beyond ±300 | 0 | 1 | 10 | **0** |

**The quietest capture of the night by a wide margin, and quieter than the bare configuration of
§3.2b.** **Against a 1 nA tunnelling current, 320 counts, the scatter is about 25 times smaller** on
these 5-sample averages. **This is the first capture in the configuration the instrument will
image in** — tip lead, cover, shields bonded — **but it is not yet a clean noise figure**, because
more than one thing changed at once.

**What changed between §3.2e and §3.2h, any of which could be the cause:**

1. **Operator distance.** Safety rule 9 exists for exactly this. **Jacob was watching the controller's
   LEDs during the §3.2e wait, and stood a metre away for this one.**
2. **Equipment in the room.** **The soldering iron was in use for the tip lead after §3.2b**, and
   §3.2d and §3.2e were both noisier than §3.2b. **If it was still on for those two and off now, it
   fits all four runs.** Unanswered.
3. **The power cycle** — the LEDs being dark at power-on shows something about the start-up differed.
4. **Time of day** in the building's electrical environment.

**Against Jacob's cable-through-one-hole hypothesis:** the routing did not change, and this capture is
quiet, so **it is not a large effect in this configuration.**

**The raw single conversions (11, 117) spread wider than √5 times the averaged spread would predict**,
as in every earlier run tonight. Recorded, not interpreted.

**Next discriminator, cheap and zero-risk with no tip:** repeat the twenty readings with **Jacob
standing where Jacob stood during §3.2e**, not touching anything, nothing else changed. A jump back up
names the operator; no change points at the room or the power cycle.

### 3.2i Two answers after §3.2h: currents by channel, and THE SOLDERING IRON WAS ON EARLIER

**`SAID`, Jacob, after the park: "-18 volts is now 0.047A and 18 volt supply is 0.06 now could be
getting +- mized up idk".** **V++ 60 mA, V-- 47 mA**, with the parked DACs and LEDs dark. **Jacob is
not sure the earlier 0.056 A and 0.07 A were assigned to the right channels.**

| | V++ | V-- |
|---|---|---|
| 2026-08-29 baseline, `docs/PROJECT_HANDOFF_SUMMARY.md` §A.1.11 | 60 mA | 36 mA |
| **Tonight, after `RSET` and `DACZ 32768`** | **60 mA** | **47 mA** |

**The C2 watch passes**: the negative channel is below the positive and steady. **V-- is about 11 mA
above the August figure**, of which the preamp accounts for about 4 mA; **the rest is unexplained**,
and whether the preamp was attached in August is not recorded. **Both channels read about 10 mA lower
after the park than at power-on.** A drop on both rails together is what op-amp output stages leaving
saturation typically do, since `RSET` and power-on leave Z at a rail. **Offered as a likely reading,
not a measurement**, and complicated by the possible channel mix-up.

**`SAID`, Jacob: "yes the soldering iron was on earlier".** **Read as: on during the noisy captures
of §3.2d and §3.2e, and off for §3.2h.** Not confirmed in those words. **It was not on for §3.2b**:
the tip lead had not yet been started. **So the iron fits all four runs**: off 45.0, on 131.5, on
294.9, off 12.7. **A temperature-controlled iron switches its heater continually, and its tip can
carry a small AC voltage**, which is exactly the kind of source a 100 MΩ input node picks up.
**Now the leading candidate, not proven**: operator distance changed in the same step.

**Two one-variable tests from the quiet state, each about three minutes:**

1. **Iron off, Jacob close** to the scan head, touching nothing. Names the operator.
2. **Iron on, in its stand where it was earlier, Jacob a metre away.** Names the iron.

**Not yet reported:** the other channel's current, the state of LED1–LED4 before and after `RSET`, and the three `ADCR` readings with their
`GSTS` control. **Record them here when they come.**

### 3.2 The computer — read off the machine by Claude, 2026-09-16

| Check | Result |
|---|---|
| `git --version` in the session's shell | **Not found** |
| `winget install Git.Git` | "Found an existing package already installed... No available upgrade found", exit 43 |
| Where Git actually is | **`C:\stm\Git`**, version **2.55.0.windows.5**. Its `cmd` folder **is** on the machine PATH; the session's own PATH did not include it |
| `python --version` | **The Store placeholder message**, not a version |
| `py --version` | **Python 3.13.15** |
| Python's own folder on the PATH | **No**, neither user nor machine. Only the launcher's folder and the Store placeholder folder |
| `bash` on the PATH | **No.** Git's `bin\bash.exe` exists inside the Git folder |
| `py -m pip install pyserial` | **pyserial 3.5** installed. A harmless warning that its helper scripts are not on the PATH |
| `git clone` | Succeeded with no sign-in. **An `ls-remote` with credentials switched off also succeeded: the repository is public** |
| **`py Code/pc/stm_console.py GSTS`, nothing plugged in** | **`No Teensy found. Ports seen:` and an empty list, exit 1. The pass** |
| `py Code/pc/check_facts.py` | **clean**, exit 0 |
| `.claude/session-start.sh` run through Git's bash | Installed the git hooks, "Up to date with GitHub", checker silent, exit 0 |

---

## 4. New faults

**None in the hardware** at the time of writing.

**Two in the tooling, both found only because this was a new machine:**

- **The commit guard and the start-up script called `python3` by name.** On this machine that is the
  Store placeholder, which exits non-zero. **Every commit would have been refused** with a banner
  saying `check_facts.py` was failing, when it had never run. The start-up script would have
  reported the checker as crashed at every session. **Both fixed.**
- **The start-up hook is launched as `bash ...`, and `bash` is not on this machine's PATH.** Whether
  Claude Code desktop finds Git's bash on its own is **UNKNOWN**: this session opened in an empty
  folder, before the repository existed, so the hook could not have run and there is no evidence
  either way. **Not changed**; see §8.

---

## 5. Corrections to earlier documents

| Where | It says | Correct |
|---|---|---|
| `STATUS.md` banner; `sessions/README.md` row for 2026-09-16; `sessions/2026-09-16.md` §6 and §8 item 2 | **"These changes are on the branch `claude/funny-cori-xttv3q`, not on `main`"** | **They are on `main`.** `1e10aa0`, that session's commit, is the tip of `main`, and `main` holds every commit on that branch. **Nothing to merge.** Corrected in `STATUS.md` and the index; the session log is history and is left as written |
| `Code/pc/README.md`, first time on Windows | **"The clone opens a browser window to sign in to GitHub, because the repository is private"** | **Public.** Cloning needs no sign-in. Pushing does |
| same | `python --version` to check, "is not recognized" means missing | **On this machine the Store placeholder answers instead**, with a different message, while Python is installed. Check with `py --version` |
| same | `pip install pyserial`, `python Code/pc/stm_console.py GSTS` | `py -m pip install pyserial`, `py Code/pc/stm_console.py GSTS` |
| same | "not yet exercised end to end" | **Followed end to end on 2026-09-16** |
| `docs/INVENTORY.md`, computers row | Jacob's machine **"not yet confirmed working"** | **Confirmed working**, with versions and locations |
| `docs/NEXT_SESSION_PLAN.md` step 0 | `python Code/pc/stm_console.py GSTS` | On Windows, `py`. Done on Jacob's machine |
| `docs/NEXT_SESSION_PLAN.md` step 3; `sessions/2026-09-16.md` §3 and §8 | **What the controller draws from ±18 V was "never recorded"** | **Recorded on 2026-08-29: V++ 60 mA, V-- 36 mA**, at a 200 mA limit, in `docs/PROJECT_HANDOFF_SUMMARY.md` §A.1.11 from that first bring-up. **Missed because the handoff body ranks last**, which is exactly the failure `CLAUDE.md` §3 warns about. Whether the preamp was attached for that reading is not stated |

---

## 6. False trails

- **"Git is not installed."** The session's shell could not find it, so a winget install was tried.
  **winget said it was already installed**, and it was, in the non-default folder `C:\stm\Git`. A
  search of the usual install folders missed it for the same reason. **The likely cause is that the
  Claude app was already open when Git was added to the PATH**, so its copy of the PATH predates the
  install. Nothing was reinstalled.
- **"Python was not found."** That message is the Store placeholder, and it appears **even with
  Python installed**. `py --version` answered 3.13.15.

---

## 7. Code and tool changes

| File | Change |
|---|---|
| `.githooks/pre-commit` | **Uses the first of `python3`, `python`, `py` that actually runs**, and refuses the commit with a plain message if none does. **Tested four ways** through Git's bash: clean tree exit 0; a planted broken reference blocked, exit 1; no Python on the PATH blocked with the no-Python message, exit 1; clean again exit 0. The planted test file was deleted |
| `.claude/session-start.sh` | **FIX 5**, same interpreter choice. Run through Git's bash on this machine: clean |
| `Code/pc/README.md` | First-time Windows section corrected from what happened: `py`, the Store placeholder, reopen the app after installing, public clone, the exact pass message, and the start-up hook note |
| `docs/INVENTORY.md` | Computers row: Jacob's machine confirmed, with versions and locations |
| `docs/NEXT_SESSION_PLAN.md` | `Last updated`; step 0 done on Jacob's machine, and `py` on Windows |
| `STATUS.md` | Header, a new banner, and the branch sentence corrected |
| `sessions/README.md` | This log indexed; the 2026-09-16 row's branch sentence corrected |

**Local to this computer, not in the repository:** `core.hooksPath` set to `.githooks` by the
start-up script, and the commit identity set for this repository only to `Claude
<noreply@anthropic.com>`, which matches every recent commit on `main`.

**Deliberately not changed:**

- **The other `python3` mentions** across the repository, counted at 23 in 15 files. They are
  prose, shebangs and usage lines, and correct on a Mac. **The README note covers Windows as a
  class** rather than editing each one.
- **The start-up hook's `bash` command** in `.claude/settings.json`. Changing it to a full path would
  break Nuh's machine and any Mac, and there is no evidence yet that it fails here.
- **No PATH or other system setting was changed.** No firmware, and no Python tool behaviour.

---

## 8. Next session

1. **Finish the list at the top of `docs/NEXT_SESSION_PLAN.md`** if tonight's run stops short, and
   put its numbers in §3.1 of this log: both supply currents, the LEDs before and after `RSET`, three
   `ADCR` readings and the `GSTS` control.
2. **At the start of the next session on Jacob's machine, check that the sync check printed**
   (`=== We-Are-STMING: sync check ===`). If it did not, the hook cannot find `bash` there: pull by
   hand, run `py Code/pc/check_facts.py`, and record it.

**Still unanswered:**

| Question | Why it matters |
|---|---|
| **Does the supply wiring read +18 V and −18 V at the unplugged plug?** | The limit setting explains the event; this rules out the wiring fault that a 200 mA limit would let do damage |
| **What does the controller draw from ±18 V with the preamp attached?** | **Measured 2026-08-29: V++ 60 mA, V-- 36 mA**, with the preamp's attachment not stated. Tonight's reading is the check against that. The negative channel is the C2 watch |
| **What does `ADCR` read with the preamp on the controller?** | The first reading taken inside the ADC's input conditions |
| **Does the start-up hook run on Jacob's machine?** | If not, sessions there start without the pull and the checker |

**Do not do:** unchanged from [`2026-09-16.md`](2026-09-16.md) §8. No `APRH`, no `CCON`, no tip, no
bare or out-of-range `DACZ`, and no bench supply on the preamp.

---

## 9. Process lessons

- **"Not found" inside an already-open program is not evidence that a tool is missing.** Check the
  machine's own PATH, or the install folder, before installing again.
- **A setting that is right for one test becomes a habit for the next.** Four days of preamp runs at
  a 20 mA limit made 20 mA feel like "what we normally run at". The plan's power-on step did give a
  different limit, but **never said the current setting was wrong for the controller**, and
  **wrongly said its draw had never been recorded**, so there was no number on the page to make
  20 mA look small. Both are now in the plan.
- **A tool that has only ever run on one kind of computer carries that computer's assumptions.** The
  commit guard had been "proved able to fail" on 2026-09-09, but only where `python3` exists. On a
  new machine it would have failed for the wrong reason, and the message would have pointed at the
  documents rather than at the interpreter.
