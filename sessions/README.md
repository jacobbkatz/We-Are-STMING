# Session logs

One file per work session. **`YYYY-MM-DD.md`, or `YYYY-MM-DD-<who or what>.md` when there is more
than one for a date** — both of us log the same day, and plans are logged separately from results.
Copy `TEMPLATE.md` to start a new one, or type `/wrap` and Claude will do it.

> **Corrected 2026-09-09.** This said "one file per work session, named `YYYY-MM-DD.md`" — a
> convention **four logs have broken since the very first session** (`2026-08-31-plan.md`,
> `2026-08-31-results.md`, `2026-09-06-plan.md`, `2026-09-09-jacob.md`). Everything that consumes
> logs was written against the singular form, so **a second log for a date fell through every
> mechanism**: the start-up hook named the wrong one, and `2026-09-09.md` was missing from this
> index entirely. `check_facts.py` now checks this index against the directory.

**Newer supersedes older** — *between* dates. Where two logs from different days disagree, the
later day is right. **Two logs sharing a date do NOT supersede each other**: they are different
sessions by different people and the filename carries no ordering. Read both. Where a log disagrees
with `docs/PROJECT_HANDOFF_SUMMARY.md`, the log is right — the handoff is known to be wrong in six
places.

`STATUS.md` in the repository root is the accumulated current state. **Read that first**; these
logs are the reasoning behind it and the record of what was ruled out.

| Session | Headline |
|---|---|
| [2026-09-17](2026-09-17.md) | **Code only, nothing powered, nothing measured.** **Second pass added two more gates for the bench: proving the leaf is really gold (flame test, no chemicals — brass blackens instantly, gold never does; aqua regia and bleach-plus-acid prohibited outright, and all chemistry kept out of the instrument's room), and cutting the leaf to size — `SAID` four 1-inch squares, which is about 100 usable 5 mm pieces rather than four attempts, against a scan range of 0.5 µm.** **The gold leaf was not in `docs/INVENTORY.md` at all until today.** Jacob with Claude Code on the web. **A bench card for mounting the gold leaf** — `docs/gold_leaf_procedure.html`, unpowered, plate off the instrument, **leading with the test of whether our copper tape's adhesive conducts**, because if it does not the gold carries no bias and no tunnelling is possible. **Then an audit and a re-analysis of the data already collected. THE APPROACH TOOL'S Z SWEEP IS ABOUT TEN TIMES TOO COARSE TO STOP IN TUNNELLING**: `DEFAULT_Z_STEP = 200` is **2.08 nm per sample point** against a detectable window of **0.21 nm**, so roughly nine approaches in ten would step from no current straight into contact — **filed as `STATUS.md` fault 6, fixed by the existing flag `--z-step 5`**, now in the plan. **It does not explain the 2026-09-16 failure** (which found nothing at all) and is waiting for the first approach that does reach the surface. **The dummy junction fit was re-derived independently from the 53 raw readings and reproduces exactly** — slope −3204.8 ± 36.5, intercept +58.0 (1.28σ), R² 0.9934, 320.48 counts/nA. **The noise conclusions survive an F-test**: the operator is not significantly worse (F=1.99 < 2.53), the iron is (F=2.87, 17.3 counts in quadrature, matching the log's "about 17") — **though "not significant" for the operator means not shown with 20 readings, and the point estimate is still 12.6 counts.** **A false trail dropped:** the motor step of 10 is NOT too coarse, because the tool sweeps Z between motor steps and 341 nm comfortably covers it. **Found in the audit: there is no CI at all** — `check_facts.py` is enforced only by a session-start script and a pre-commit hook, both on the developer's own machine, both bypassed by `--no-verify`. Recommended, not added |
| [2026-09-16 showcase](2026-09-16-showcase.md) | **THIRD session filed under 2026-09-16, a different session from both `2026-09-16.md` and `2026-09-16-bench.md`, which it does not supersede.** Jacob with Claude Code on the web. **Nothing powered, nothing measured** — a cloud session cannot reach the Teensy. **A public progress page was built, `docs/progress.html`**, assembled from `STATUS.md`, `docs/FACTS.md` and the session logs, and published as a shareable link to be regenerated weekly. It carries the full dummy-junction sweep — all 53 raw readings, transcribed and re-totalled, all eight means reproducing §3.4 exactly. **Found on the way: `docs/preamp_next_steps.html` still told the bench the Keystone 11301 "drops into our hole"** — retired at the bench 2026-09-15 — **while the same page carried the correction 45 lines below it.** Corrected. **The cause is that `check_facts.py` never opened an `.html` file at all**: its filter listed `.md`, `.py`, `.hpp`, `.cpp` and `.txt`, so all five HTML documents in `docs/`, four of them live bench instructions, were invisible to the guard. **`.html` added and proved in both directions** — red on the real defect, red again on a planted `800 counts per nA` inside the new page, green with both gone. **The `Images/*.bmp` scans were deliberately left off the page: they are Mech Panda's reference scans, not our data.** No stage, fault or safety rule changed |
| [2026-09-16 bench](2026-09-16-bench.md) | **Second session filed under 2026-09-16, a different session from `2026-09-16.md`, which it does not supersede.** Jacob with **Claude Code desktop on Jacob's Windows machine**, the restart the web session asked for. **The machine works**: repository cloned, pyserial installed, dry run passed. **Type `py`, not `python`, there**: `python` and `python3` are Microsoft Store placeholders. **The commit guard and start-up script called `python3` by name and would have refused every commit on this machine**; fixed and proved in both directions. **First controller power-on: one supply channel went into current limit at about 2 V and both were switched off. That channel's limit was 20 mA, the other's 200 mA**; the 20 mA was left over from the preamp tests, while **the controller draws 60 mA and 36 mA, measured 2026-08-29** — a figure the plan wrongly said had never been recorded. **At 200 mA it ran at about 40 mA. Then THE ADC CHAIN WORKS**: USB needed its plug fully seated; after `RSET` with LEDs dark and Z parked, **twenty `ADCR` readings average −1.7 counts with no drift**, matching the previous night's −0.4 mV meter reading, **the first ADC reading in the project inside the converter's input conditions.** The spread is not a noise figure with the shield cover off, and a known signal through the preamp is still owed. **Then the tip lead was built**: standoff beeps to tip holder, holder open to the piezo brass, no tip fitted, IPA-cleaned, slack. **Re-measured: mean −22.3 counts, under one standard error from before, so it passes with any added leakage below about 0.2 nA — but the spread tripled to 131.5 counts, about 0.4 nA**, with the cover off. **Then the shield cover went on with both shields bonded to "universal ground", undefined in this repository — and the spread more than doubled, to 294.9 counts, about 0.9 nA**, with readings bunched near ±480 as mains hum would be. ~~**Next: each shield's one bond moved to `AGND`**~~ — **wrong: "universal ground" is the supply channels' junction, which IS `AGND`.** **After a break, the same hardware with Jacob a metre away gave a standard deviation of 12.7 counts, about 40 pA — the quietest capture of the night.** Operator distance, a possibly forgotten soldering iron, the power cycle and the time of day all changed at once, so **the cause of the earlier noise is not yet named**. **Two one-variable tests followed: Jacob standing close with the iron off gave 17.9 counts; the iron hot with Jacob away gave 21.5.** **Neither is the big noise source**, though the iron adds a significant ~17 counts, so it stays off for captures. **Leading candidate now: the freshly soldered, IPA-cleaned input node still drying**, untested. **The dummy junction test was chosen next and waits on clip leads.** **Then firmware fixes for both tip-crash faults — `CCON`'s jump to midscale and the motor left powered — were written and built with PlatformIO, NOT uploaded**; a red-then-green bench test is in the plan. Found on the way: `RSET` never switched the motor off, and `CCON`'s target is unbounded. **Then, with clips bought, THE DUMMY JUNCTION TEST PASSED**: a 100 MΩ resistor from the bias wire to the tip holder gave **−3,205 ± 37 counts per volt against −3,200 predicted, 320.5 counts per nA**, R² 0.993, no drift. **The whole current path works end to end for the first time, and the sign is measured: positive sample voltage reads NEGATIVE.** **Then the two firmware fixes were uploaded and bench-tested, red first: `CCON` no longer jumps Z (held at 20000, where the old firmware went to 32768), and the motor coils switch off after every move. Faults 2 and 3 FIXED.** **Last, the motor direction: positive `MTMV` pushes the driven screw out, and the design mesh puts the tip holder 1 mm on the driven screw's side of the pivot — the notes had said the other side — so NEGATIVE approaches, provisionally.** **A 3-turn move was sent without asking the range and could not be stopped by command**; it finished harmlessly and was returned to base. **Then a tip was fitted, and the Z direction was worked through: it cannot be settled from the repository** — it hangs on the disc's unknown poling and which face the tip is on. **`stm_approach.py` gained `--z-retracted unknown`**, parking Z at midscale for motor steps and learning the direction at first contact; 50 tests, each new one shown to fail against an injected fault, which also exposed two blind spots in the test harness itself. **Last, the FIRST APPROACH ATTEMPT**: gold leaf in front of the tip (Claude misread a photo as saying otherwise), plate-to-ground and plate-to-tip silent, gap closed by hand, touch check at −0.5 V clean. **The approach tool's reads were measured at 0.208 s each and fixed to 0.5 ms; contact now needs 2 of 3 reads.** **−2000 steps negative, then, on Jacob's call that the tilted plate means positive approaches, +6144 positive past the hand-set start: no contact.** Bias zeroed, Z parked, **screw left at +6144 steps**, powered down. **Next: settle the motor direction by looking at the tip against the pivot line, check the gold is joined to the bias, and find contact by hand before backing off a known amount** |
| [2026-09-16](2026-09-16.md) | **Setup only, nothing powered, nothing measured.** Jacob at a Windows computer new to the project, using Claude Code on the web. **A web session runs in the cloud and cannot reach the Teensy's USB port**, so the first controller power-up did not happen; every earlier bench session ran Claude locally on Nuh's machine, and "the Windows machine has run this before" meant that one. Jacob stopped to install Claude Code desktop, Git and Python and restart there. **First-time Windows setup written into `Code/pc/README.md`**, a computers row added to the inventory, the plan corrected to say whose machine and that Claude must run on the computer the Teensy is plugged into. The bench list was checked node by node and is unchanged. ~~**This session's changes are on the branch `claude/funny-cori-xttv3q`, not on `main`**~~ [**corrected later 2026-09-16: they are on `main` as `1e10aa0`; nothing needs merging**] |
| [2026-09-15 bench](2026-09-15-bench.md) | **THIRD session of this run, second filed under 2026-09-15 — a different session from `2026-09-15.md`, which it does not supersede.** **THE PREAMP WORKS.** Jacob at the bench: the input node was built, the board was powered for the first time in this project, mounted in the new copper box, and **reads about 4 pA at 45 minutes** against a 1 nA target — **250× inside safety rule 14's gate, which is now satisfied.** **Safety rule 0's 45-minute warm-up is RETIRED**: zero change between 20 and 45 minutes, twice, so the old board's climb was its floating reference charging, not the preamp. **The box and shield cost nothing** — 4 pA boxed against 6 pA bare. **The Keystone 11301 does NOT enter our hole**, after three web-sourced answers all said it did, so the standoff rests on the board surface held by the resistor lead. **A gloved touch on the input gives 0.5 nA** against 20–50 nA from a metre away on the old board. **V1 closed: all shielding metered end to end.** **C2 will not be replaced — Jacob's decision**, and safety rule 14 clause (a) is withdrawn. **Caught before power-on: both supplies were set to 18 V**, which is the controller's figure, not the preamp's. **The preamp was then mounted in the box and spliced to the controller cable, and the splice was verified unpowered** — row of five reads nothing, white, orange, tan, grey, which is correct. **Nothing has been powered through the controller yet.** **An expected reading Claude wrote was wrong and a correct splice looked like a fault**: white was said to beep to all four ground pins, but with the connector unplugged it beeps to exactly one. **Caught only because Jacob reported what he saw rather than what he expected.** Against that: **six readings were reported against eight checks**, and the bench supply's rails wandered once with the cause never captured |
| [2026-09-15](2026-09-15.md) | **Second session of the same night as 2026-09-14** — it ran past midnight, so neither supersedes the other. **Jacob at the bench, nothing powered. Part B passed: all six lead free-end readings as expected**, the last unpowered gate. **The spare board was then washed in 99% IPA and left to dry overnight** — the first time any preamp board in this project has been cleaned. **JP1 lead colours recorded for the first time:** hole 1 GND white, 2 +15 V grey, 3 OUT orange, 4 empty, 5 −15 V tan. **Two inventory corrections: there are NO calipers, and no oven or dehydrator was ever bought.** That second one killed the distilled-water rinse — without a bake, water under IC1 is a leakage path that would mimic the warm-up drift we cannot yet explain. **The wash is IPA only, and is now written out properly with the bath method.** Part C skipped on the night by Jacob and **resequenced rather than dropped**, to after the wash and before the input node; his objection to "both channels within 1 mA" was correct and the criterion is fixed. Against that: **the "press fit, drill it out" claim that commit `04b15a4` reported fixed was still live in SIX more files**, including a `#44 drill` line in the shopping list and the bullet in `CLAUDE.md` every session reads first. **And `check_facts.py` has three defects, one of which silently kills a RETIRED row whose replacement shares a number with the retired value** — found only by deliberately re-introducing the fault. None fixed; all recorded |
| [2026-09-14](2026-09-14.md) | **Jacob at the bench alone, nothing powered. The spare preamp board's ground repair is finished and verified — `IC1` pin 3 beeps to hole 1**, the first time in this project the amplifier's 0 V reference has been connected to anything. Wires 3 and 4 fitted (wire 3 had never been on the board), then **eight continuity readings and one capacitance reading, all pass**. **The +15 V clearance hazard at C3's via is closed — not bridged**, settled by hole 2 reading silent with wire 4 fitted. **C2 survived the two-iron rework**: hole 1 to hole 5 reads 4.7 µF, against near zero before. **No new faults — the first bench session in this project with none.** Against that: **"holes 2 and 5 chirp only" was wrong and was written in three places.** The chirp lasts `C × R_threshold`, 96–960 µs at 4.8 µF, below what a beeper can sound — **silence is the normal reading, and a fully working board briefly looked faulty.** The bench page also had no "Silent" button on those two rows, so the reading that actually occurred could not be recorded. **Nothing powered; a standoff arrived but which one is UNKNOWN** |
| [2026-09-13](2026-09-13.md) | **Nuh at the bench, nothing powered. IC1 pin 3 — the preamp op-amp's 0 V reference input — is connected to nothing, on BOTH boards**, measured and confirmed through a scraped via. **The fabricated boards have no ground pour**: Berard's Eagle board has one, the KiCad gerbers JLCPCB built from do not, leaving six GND points dead (C2, C3, C4, IC1 pins 3 and 8, JP1 pin 4). **So every preamp reading in the project, including the "119 nA", was taken with a floating reference and cannot be read as a current.** C2 on the spare found reversed and rotated (original part reused, 4.65 µF, 35 V — replace before the controller). Four leads on; ground repair 2 of 4 wires done, not yet verified |
| [2026-09-09 Jacob](2026-09-09-jacob.md) | **The long one. Nothing measured, nothing powered.** C2's reverse connection re-verified and **shown it cannot cause the 119 nA**; **we own the Keystone 11311, not the 11301**; new `docs/INVENTORY.md` and `CLAUDE.md` §3d. Then purchases recorded, the piezo closed as **built and working**, and **M2x6 not M2x8** measured out of the box mesh. Then four audit passes into why the answers had got worse: **the first three blamed the repository and were wrong** — the start-up hook named the *right* log and the answer was the first row of a printed table. **The real mechanism: nine errors across three sessions and both people's work, all the same move** — act on the first plausible reading, write it in as fact, skip the cheap check. Model and context ruled out by measurement. **Mechanism added, not more rules:** a **pre-commit hook refusing a commit while `check_facts.py` fails** (auto-installed on both computers) and a **checker that detects its own blind spots**, generalising a finding of Nuh's that a badly worded RETIRED qualifier let it report clean while three files carried the wrong number |
| [2026-09-09 Nuh](2026-09-09.md) | **Materials and technique for finishing the preamp. Nothing measured, no hardware powered.** Found that **the preamp box already fits the board** so no reprint and no glue are needed, that **`1_preamp_box_base_v2_screwmount.stl` must not be printed** because its added boss lands on the PTFE standoff hole, and that **C2 is reverse-connected in the source design**. Established that the flux we own is the wrong chemistry for a 100 MΩ input node and chose the replacement. **Was missing from this index until 2026-09-09** |
| [2026-09-08](2026-09-08.md) | **Repository audit and migration. Nothing measured, no hardware powered.** Jacob flagged the repository as convoluted; it was measured before anything was changed — 12,586 lines across 34 files, with the real problem being **duplication, not size**. Two values corrected on 2026-09-07 still sat wrong in **six live documents**. New **`docs/FACTS.md`** (one canonical value per number, with a RETIRED table) and **`Code/pc/check_facts.py`**, which runs at every session start and on its first run found **a live code bug**: `stm_approach.py` still defaulted to the retired 4.096 V full scale and would have printed every current **2.5x too small** on the first real approach. Superseded material archived with banners, `sessions/` additive only. **Two of the audit's own recommendations were reversed on inspection** — archiving the cold-start procedure, and merging the `CLAUDE.md` rules. Sections **37 and 38 were misfiled into `2026-09-07.md` and moved here verbatim** |
| [2026-09-07](2026-09-07.md) §17-27 | **Nuh at the bench — first hardware measurements since 31 August, and they overturn the project's headline number.** A meter on PAD1 reads **+11.905 V**, not the assumed 3.73 V: the leak is **~119 nA, not 37 nA**. R23 confirms at 11.914 V, so there is no attenuation and **the ADC is being driven ~3x past its ±4.096 V span** [**corrected 2026-09-08: the span is ±10.24 V, not ±4.096 V — see `docs/FACTS.md`. The "~3x past span" conclusion does not follow from the corrected number and should not be carried forward; what the ADC actually sees is the differential PREAMP+ minus PREAMP-, and PREAMP- is floating, so it is not known**]. Worse: **`PREAMP-`, the ADC's differential reference, is FLOATING** — every ADC reading in the project has been referenced to a drifting node, and this is the same "one JP1 ground is open" fault filed as a loose end on 31 August. **The leak is on the preamp board** (coax, tip holder and tip eliminated by measurement). The **rail-scaling prediction failed** — a 39% cut in the −15 V rail produced no proportional change. **The board takes 45+ minutes to settle** after power-on, ~30 nA of drift nobody had controlled for. **The DAC dropout is not time-triggered.** Three of the session's own results are in doubt because of the floating reference |
| [2026-09-07 audit](2026-09-07.md#second-pass-technical-audit-2026-09-07-late) | **Component-level datasheet pass and second-pass audit.** New `docs/COMPONENTS.md` and `docs/NEXT_SESSION_PLAN.md`. **The DAC floating-pin hypothesis is very likely dead** — RESET has an internal pull-up, and the power-sequencing explanation was in the handoff all along. **`PREAMP−` floating is a spec violation**, not just an inconvenience: the ADC needs IN− within ±500 mV of GND and ours sits at 2.7 V. **The noise may be void entirely** — it is 1,700x the Johnson floor and 10,000x the op-amp's, so it is probably the floating reference. The TIA has **no compensation capacitor** and its real bandwidth is ~5 kHz. 119 nA vs the OPA627's 1 pA rules the op-amp out |
| [2026-09-07](2026-09-07.md) | **Superglue was used twice, and the first use is the serious one** — a little bonds the **PTFE standoff to the board**, straight across the insulator that holds the input node. New leading candidate for the 37 nA. The **output sign says the leak is to the negative rail** (~402 MOhm needed; `IC1` pin 4 is 2.54 mm away). The **cut coax enables a free bisection test**. The noise is **several hundred times** the Johnson floor. Print material settled: **PETG-CF**. **Addendum:** the bias was on all along (my error) — and working that through gives the sign table showing **every other voltage in the instrument has the wrong sign**, leaving only the −15 V rail. **The piezo is superglued into its socket too** — a third CA site, and a possible mechanical stiffening of the scanner |
| [2026-09-06 plan](2026-09-06-plan.md) | **Bench plan for Nuh working alone.** Meter checks on the floating DAC reset pins, the shield, and tip isolation; the 30-minute DAC idle test; shield grounding with before/after captures; a new rail-scaling test; motor direction |
| [2026-09-06](2026-09-06.md) §13-22 | **Nuh at the bench, meter only, nothing powered.** All eight CLEAR#/RESET# pins measured **open on the physical board** — third independent confirmation, with controls. **The preamp shield was discontinuous and only partly grounded**, not "never grounded" as recorded: a ground wire had been added after 31 August and never written down. That would have made Block D3 return a false negative and pushed us into consuming the spare preamp board. **Shield stripped and rebuilt in all copper, seams soldered — continuity NOT yet verified** |
| [2026-09-06](2026-09-06.md) | **CLEAR# and RESET# are floating on all four DACs** — the best explanation yet for the configuration loss, and a four-wire fix if confirmed — later **confirmed from the manufacturing netlist**. The gerber archives turned out to contain complete netlists for both boards: **JP1's pinout is resolved and the "do not run a wire" rule is retired**. The 37 nA candidates ranked by impedance: contamination beats the floating shield. A new cheap test: the offset should scale with rail voltage if it is rail leakage |
| [2026-09-05](2026-09-05.md) | Cross-referenced Dan Berard's build against our source. **Two unfixed tip-crash risks found in firmware**, both confirmed present in Mech Panda's upstream too: `CCON` snaps Z to midscale, and the motor is left energised. Coarse approach step size is ~7.8 nm, not the 244 nm previously assumed. **`stm_approach.py` written and tested** (40 tests, never run on hardware). Mech Panda's repo has **no licence at all**. Sample decided: gold foil. **Third pass: the ADC full scale question is settled at 4.096 V from the controller schematic we already had** — every current figure the Python tools print is 2.5x too large. No public replication of this build exists. **Fourth pass:** Berard's scan head page closes tip mounting, sample mounting and the piezo reference part, and shows he quotes the lever ratio as both 20 and 30 — so the step size is 5–8 nm, not 7.8 |
| [2026-09-01](2026-09-01.md) | Repository restructured for two-computer working. **The preamp case shield was never grounded** — a second candidate for the offset — recorded then as 37 nA, **superseded, it is ~119 nA** — that a board rebuild would not fix. **The shield was later eliminated as an offset source by measurement (2026-09-07)** |
| [2026-08-31](2026-08-31-results.md) | Preamp fault characterised: leakage recorded then as 37 nA — **superseded, it is ~119 nA; the old figure was a scale error, not a smaller fault** — and the loop closed not open. Bias path passes, first test ever. **Two new faults:** DACs lose configuration hourly, one JP1 ground open. ADC clock 40 MHz to 1 MHz. Piezo has no usable resonance when mounted |
| [2026-08-31 plan](2026-08-31-plan.md) | The plan that session ran against, including its Part 0 corrections to the handoff |

## A stray branch on the remote

`origin/session-2026-08-31` holds two commits by Nuh from 31 August, on a **separate history**
(it descends from Mech Panda's original repository, not from ours):

| | |
|---|---|
| `d8069d5` | Session 3: ADC clock fix, bias path verified, preamp fault identified |
| `0f9515d` | **Resolve JP1 pinout from the preamp Gerbers: pin 4 is unrouted** |

**All of its content is already on `main`**, verified file by file on 2026-09-06 — it arrived via
Jacob's manual upload on 1 September, at the reorganised paths. The only differences are those path
updates and line endings.

**It is safe to delete, but has been left alone** because it is Nuh's branch and both of us were
not present. Delete it together, or leave it as an archive — but **do not push to it**, or work
will diverge onto a history `main` cannot see. That is how the JP1 finding nearly went unnoticed:
it was on `main` all along, in the handoff's header, and a later session re-derived it from scratch.

---

Sessions 1 and 2, on 2026-08-29 and 2026-08-30, predate this directory. They are written up as
Appendix A of `docs/PROJECT_HANDOFF_SUMMARY.md`.
