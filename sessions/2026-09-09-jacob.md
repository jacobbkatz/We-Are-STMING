# Session 2026-09-09 (Jacob, remote)

**Who was there:** Jacob, remote, in Spain. No hardware present, nothing powered.
**Type:** code and documentation only — **no measurement was taken**
**Goal for the session:** verify the C2 finding, produce a purchase list, and diagnose why this
session made repeated context errors

> **Two logs share this date.** `sessions/2026-09-09.md` is Nuh's, written first. This is Jacob's,
> later the same day. Neither supersedes the other; they cover different work.

---

## 1. Headline

| | |
|---|---|
| **Nothing was measured.** No hardware powered | Every number below comes from a manufacturing file already in this repository |
| **C2 is confirmed reverse-connected in the design** — independently re-derived | Schematic, gerber pin attributes, **and** the Eagle pin-to-pad mapping, which had not been checked |
| **The board has NO polarity marking on the silkscreen** | New. Removes "they fitted it against the silkscreen" as an explanation — there was nothing to fit it against |
| **C2 polarity CANNOT cause the 119 nA** | The input node is galvanically isolated, and C2's pads sit at −15 V and GND whichever way the part faces. Reliability item, not a lead |
| **We do not own the Keystone 11301.** We own the **11311**, which does not fit | `docs/BOM.md` said CONFIRMED and the shopping list said "already have". **Both were wrong** |
| **The preamp boards came PCBA from JLCPCB, 2 off — and JLCPCB flagged C1/C2 polarity at order time** | Neither fact was written down anywhere. Both came from Jacob in conversation |
| **New `docs/INVENTORY.md` and `CLAUDE.md` §3d** | The root cause of this session's errors: nothing recorded what we physically own |

---

## 2. State at a glance

**No stage changed.** No hardware was touched.

---

## 3. Measurements

**None.** Figures below are read from manufacturing files.

### 3.1 C2, verified three ways

| Source | Result |
|---|---|
| Eagle schematic `tunnelAmp.sch` | `-9V : C2.+, C4.1, JP1.5, IC1.V-` and `GND : C2.-, C1.-, ...` |
| Manufactured gerber `tunnelAmp-F_Cu.gbr` X2 attributes | `%TO.P,C2,+*%` on `%TO.N,-9V*%` at X 9.7575; `%TO.P,C2,-*%` on `%TO.N,GND*%` at X 15.0075 |
| **Eagle device pin-to-pad mapping** — **not previously checked** | `<connect gate="G$1" pin="+" pad="+"/>`. **No swap.** The library is not the explanation |

C1 and C2 are the same package (`C/6032-28W`), same rotation (R180), same X. **C1 is correct**
(anode on +15 V). The nets are named ±9 V but the board is fed ±15 V.

### 3.2 There is no polarity marking on the silkscreen

`tunnelAmp-F_Silkscreen.gbr` has exactly two flashes at each capacitor, both **aperture D16**, a
plain 2.75 × 1.8 mm rounded rectangle — one per pad, identical. The Eagle footprint does carry an
anode stripe, but as a `rectangle` on **layer 51 (tDocu)**, which is never plotted to silkscreen.

**Consequence:** neither a machine nor a human had anything on the board to orient the part by.
This is consistent with Jacob's recollection that **JLCPCB raised the polarity of C1 and C2 at
order time.**

### 3.3 C2 pad geometry, from the gerber

Aperture `%ADD11R,2.750000X1.800000*%`, flashes at X 9.7575 and 15.0075, Y 2.2225.

| | |
|---|---|
| Pad size | **2.750 × 1.800 mm** |
| Pad centres | **5.250 mm** apart |
| Gap between pad inner edges | **2.500 mm** |

**A 1206 or 1210 replacement (3.2 mm long) lands only 0.35 mm on each pad. An 1812 (4.5 mm) lands
1 mm.** The shopping list said 1206/1210 and has been corrected.

---

## 4. New faults

**None in hardware.** Two documentation faults, in section 5.

---

## 5. Corrections to earlier documents

| Where | It says | Correct |
|---|---|---|
| `docs/BOM.md` §3, `docs/PREAMP_SHOPPING_LIST.md` "Already have" | PTFE standoff terminal **Keystone 11301**, "CONFIRMED 2026-09-05" / "already have" | **We do not own it.** DigiKey salesorder **100750867**, 2026-08-02, is **36-11311-ND ×2** — the **11311**, which needs Ø3.45 mm against our Ø2.108 mm. **Tried at the bench; it does not fit.** The 11301 is now on order |
| `docs/FACTS.md` | Keystone 11301 mounting hole **Ø2.184 mm (0.086")**, "press fit with 0.076 mm interference" | **VERIFY.** 0.086" is also listed as this part's *turret head* diameter, and a PTFE-insulated terminal's hole must clear its insulator, so both cannot be right. Demoted, not deleted |
| `docs/PREAMP_SHOPPING_LIST.md` | C2 replacement **1206/1210** | **1812.** Measured from the gerber — see §3.3 |
| `STATUS.md` fault 1d | "It is possible whoever assembled it fitted the part against the silkscreen and got it right" | **There is no silkscreen polarity marking.** That explanation is not available |
| `STATUS.md` fault 1d, next action A | Framed as a required pre-power blocker, fire risk | **Retoned.** It cannot cause the 119 nA (§6.1), and the board has run powered for weeks on a healthy rail. Still worth checking; not a blocker |
| `STATUS.md` fault 1d | "Rotating the tantalum 180° also works" | True, but **prefer replacing.** If reversed it has sat at 15 V reverse for weeks and its dielectric is degraded. **And: C1 is correct — a warning not to rotate C1 has been added** |
| `STATUS.md` next action 2 | "See `docs/FACTS.md` and `docs/FACTS.md`" | Duplicated link, one reference lost. Deduplicated |
| `docs/BOM.md` §3 | Read as a parts-to-buy list | **It is a specification.** The boards came PCBA from JLCPCB with IC1 and C1–C4 and R1 fitted. Banner added |

---

## 6. False trails, and things ruled out

### 6.1 C2 polarity is not the 119 nA. Ruled out by argument, not by measurement.

`N$4` has **exactly one connection in the entire design** — `IC1.-IN`. One pad, no track, no via.
C2 connects the −15 V net to GND and touches neither.

**And C2's two pads sit at −15 V and 0 V whichever way the part inside is facing.** The copper,
the soldermask and any contamination on them see an identical voltage map either way, so the
surface-leakage path toward the input pad is unchanged. The only thing polarity alters is leakage
*inside* the capacitor, anode to cathode, which flows −15 V to GND entirely within the supply loop.

**Nor the other symptoms:**

| Symptom | Why C2 does not explain it |
|---|---|
| 45-minute warm-up drift | C2 sits **7.1 mm** from IC1. A 6032 part on a 20 × 15 mm board settles thermally in a minute or two, not 45. Raising it 1 °C needs roughly 10 mW — about **0.7 mA** of leakage, obvious on the bench supply |
| Noise | OPA627 PSRR is over 100 dB, and the recorded noise is probably the floating `PREAMP−` |
| Anything at all | The −15 V rail measured **−15.237 V** on 2026-09-07. A significantly leaking C2 would load it |

**Do not re-rank the 119 nA candidates because of C2.** The superglue at the PTFE standoff is
unaffected and remains the lead.

### 6.2 Continuity testing cannot detect capacitor polarity

Jacob recalled testing continuity at C2. **A capacitor is a DC open in both directions**, and
buzzing its terminals identifies which *net* each pad is on — which is the same either way. Only
reading the stripe under magnification settles it. The earlier test was not wrong, it was the
wrong instrument for this question.

### 6.3 An inference from document structure, presented as a finding

`docs/BOM.md` §3 lists preamp parts individually and `gerbers/README.md` prices only *controller*
assembly, so this session concluded the preamp board was hand-populated. **It was JLCPCB PCBA.**
See §8.

---

## 7. Code and tool changes

| File | Change |
|---|---|
| **`docs/INVENTORY.md`** | **New.** What we physically own, with provenance — order numbers, bench observations, "Jacob said so", each dated. Plus the known gaps |
| **`CLAUDE.md` §3d** | **New rule.** What we physically own is not derivable from any design file. The BOM is a specification. Ask, or write UNKNOWN — never infer |
| `CLAUDE.md` §6 | `docs/INVENTORY.md` added to the ownership table and the file map |
| `docs/BOM.md` | Standoff row corrected; §3 banner saying it is a specification, not an inventory |
| `docs/FACTS.md` | 11301 hole demoted to VERIFY; 11311-as-owned row added; edge margin now includes the 0.815 mm figure for Ø3.45 |
| `docs/PREAMP_SHOPPING_LIST.md` | Standoff moved from "already have" to "buy"; ceramics 1206/1210 → **1812**; C1 warning; PCBA note; #44 caveat |
| `docs/OPEN_QUESTIONS.md` | Three new: the 11301 hole diameter, what else JLCPCB left off, how the C1/C2 polarity question was answered |
| `STATUS.md` | Fault 1d rewritten; next action A retoned; standoff correction in the rebuild step; duplicate link fixed |

**Deliberately not changed:** session logs; the 119 nA candidate ranking; any electronics or
engineering conclusion. Nothing here is a new engineering claim except §6.1, which is an argument
from the netlist, not a measurement.

---

## 8. Process lessons — why this session made repeated context errors

**Jacob, plainly:** *"you've gotten a lot dumber and less intelligent since our last session."*
Worth recording honestly, because the diagnosis is actionable.

**Checked, not guessed:** the model did not change. `configured_model` and `last_served_model` both
`claude-opus-5`, effort high, no fallback.

**What did change: this session was created 2026-09-01 and had been compacted** — it ran out of
context and was replaced by a summary. The raw conversation was gone.

**The four errors all have the same shape.** Each was a *physical* fact — what we own, how a board
was assembled, what a supplier did — inferred from a *design* document and stated before being
flagged as an inference:

| Inferred | From | Actually |
|---|---|---|
| Preamp was hand-populated | `docs/BOM.md` layout | **JLCPCB PCBA, 2 off** |
| We own the 11301 | `docs/BOM.md` "CONFIRMED" + shopping list | **We own the 11311** |
| Nobody had checked C1/C2 polarity | nothing said otherwise | **JLCPCB raised it at ordering** |
| (before compaction) A v2 box boss position was safe | the mesh | **It landed on the input node.** Caught by Nuh |

**The root cause is not the model and not the compaction. It is that nothing in this repository
recorded what we physically own.** All seventeen documents were checked. `docs/BOM.md` records what
was *specified*; a netlist records what was *drawn*; a shopping list records what someone *intended
to buy*. **None of them is an inventory**, and reading one as if it were produced every error above.

Those facts lived in Jacob's head and in a Gmail thread. When the context was compacted they went
with it, and the gaps got filled by inference.

> **`CLAUDE.md` §3b says search before declaring something unknown. §3c says get the number before
> ordering a test. §3d, added today, covers the case neither reaches: the answer is in nobody's
> file. Ask, or write UNKNOWN. An order confirmation outranks every document in this repository.**

**A second, smaller lesson.** Purchase advice was given, reversed one message later, and reversed
on the standoff too. **Reason a purchase decision all the way through before putting it in front of
someone who is about to spend money.** A correction costs them a second order and shipping.

---

## 9. Next session

Unchanged from `docs/NEXT_SESSION_PLAN.md`, plus:

1. **Look at C2's stripe under the magnifier before reworking anything.** Two minutes. **Do not
   touch C1.** Not a blocker — see §6.1.
2. **Calipers on the 11301 when it arrives**, and on the board hole. Closes the VERIFY in
   `docs/FACTS.md`.
3. **Fill in `docs/INVENTORY.md`'s gaps** — what else JLCPCB left off, and whether we own M2
   screws, copper tape and heat shrink.
4. **Consider starting a fresh session.** This one is nine days old and has been compacted once.
   With the facts now written down, the repository can do the remembering.

**Do not do:**

- **Do not rotate or replace C1.** It is correctly connected.
- **Do not open the standoff hole to fit the 11311.** 0.815 mm of board to the edge.
- **Do not treat C2 as a candidate for the 119 nA.** §6.1.

---

## 10. Piezo solder — which one to buy, added later the same day

**Jacob asked which solder to buy, specifically a syringe rather than a jar.** Documentation only,
nothing powered, nothing measured.

### 10.1 There are two solders in this project and they are not interchangeable

This took a moment to untangle because the repository says both "do not buy solder" and "buy one
syringe of solder paste", and both are correct — they are about different joints.

| Job | Alloy | Status |
|---|---|---|
| **Everything electrical** — JP1, the turret, the tip lead, the shield seams | Sn99.3/Ag0.3/Cu0.7 lead-free wire | **We own it. Do not buy more.** `docs/INVENTORY.md` |
| **The four piezo quadrant wires, and only those** | **Sn42/Bi58 low-temp paste** | `docs/BOM.md` §5 asks for one syringe. **Not in `INVENTORY.md` — nobody has said whether we own any** |

**The syringe Jacob was asking about is the second one.** It is the only paste in the build.

### 10.2 The apparent contradiction about bismuth, resolved

`sessions/2026-08-31-results.md` rule 4 says **"do not use bismuth low-temp solder"**, and
`docs/PREAMP_SHOPPING_LIST.md` repeated it in its "Do not buy" line without a scope. Read cold,
that flatly contradicts `docs/BOM.md` §5.

**Both are right. The 08-31 rule is about the preamp board, where the list it sits in is titled
"Rules for the rebuild".** Its two reasons were that bismuth is brittle, and that bismuth meeting
leaded solder forms a eutectic near 96 C. Neither reason reaches the piezo:

- **Brittleness.** A quadrant wire is a 40 AWG hair glued flat to a disc that never sees mechanical
  load. Brittleness matters at a connector or a board that flexes, not here.
- **The 96 C eutectic.** That needs lead. **We own no leaded solder** — ours is Sn99.3/Ag0.3/Cu0.7.
  The hazard cannot arise in this workshop as currently stocked. **If leaded solder is ever bought,
  this reactivates**, which is why it is now a row in `docs/FACTS.md` rather than a line in one
  session log.

Both files have been scoped so the next person does not have to re-derive this.

### 10.3 The product

**Chip Quik `SMDLTLFPT4`** — Sn42/Bi57.6/Ag0.4, 138 C, **5 cc / 15 g syringe**, T4 powder.

Chosen over the unbranded 30-50 g listings for three reasons:

1. **It has a published datasheet with a flux classification (REL0) and a stated shelf life.** The
   unbranded ones state neither. On a build whose blocking fault is surface contamination, an
   unspecified flux is not a small unknown.
2. **The Ag0.4 addition answers the brittleness objection directly.** Micro-alloying silver into
   Sn-Bi is the standard industrial fix for exactly the failure mode 08-31 raised. Same 138 C.
3. **15 g in a 5 cc syringe is the small one.** Four joints need milligrams. **Shelf life, not
   quantity, is the binding constraint**, so a bigger tub is worse, not better value.

**Amazon and chipquik.com were both blocked by the network egress proxy, so the price and stock
could not be confirmed.** DigiKey lists it as part 8543525. **VERIFY the price before ordering** —
a third-party figure of about $22 was the only number visible and it is not a source to trust.

### 10.4 The shelf-life trap, which changes when to buy

Chip Quik's own figures: **refrigerated at 3-8 C, better than 6 months; unrefrigerated, better than
2 months.** It needs 4 hours to come back to room temperature before use.

**The preamp is the blocker and the piezo work is not next.** Ordering this today and leaving it in
a drawer means it is likely out of life before it is opened. Either buy it with the piezo discs when
that work actually starts, or buy it now and **put it in the fridge the day it arrives.** Not the
freezer.

### 10.5 The iron temperature — an open question since 2026-09-05, now largely closed

`docs/OPEN_QUESTIONS.md` §4 has carried "actual iron temperature and dwell for our discs" as
undocumented. **It was answerable from arithmetic already in the repository.**

The tip is the only heat source, so nothing in the assembly can end up hotter than the tip. **Set
the station below the depolarisation ceiling and the ceramic cannot reach it however long the iron
is held there.** That removes dwell from the problem entirely and leaves one dial setting. The
melt is 138 C, the ceiling about 210 C: **set 190 C.**

**What is still VERIFY, and it is the whole remaining risk:** displayed temperature is not tip
temperature, and we own no tip thermometer. A station reading 20 C low sits exactly on the ceiling.
**The mitigation is a sacrificial disc** — which is why `docs/BOM.md` §5 already says buy five or
more. **A ruined disc still reads correctly on a meter**, so that test has to be mechanical, not a
resistance check.

Full derivation and its caveats are in `docs/OPEN_QUESTIONS.md` §4. Values are in `docs/FACTS.md`.

**This also replaces "low temperature and short dwell" as the stated technique.** Short dwell was a
proxy for keeping the ceramic cool, and it is a poor one — it puts a beginner under time pressure at
a delicate joint. **Set the dial right and take your time instead.**

### 10.6 The flux on this paste is the no-clean type we just rejected — and it still matters here

Chip Quik's low-temp pastes carry a **synthetic no-clean flux**, which is precisely the chemistry
`sessions/2026-09-09.md` §10.2 ruled out for the preamp because its residue does not fully dissolve
in IPA. **No rosin-fluxed Sn42/Bi58 paste could be found; that is recorded as not-found, not as
does-not-exist.**

**Why it is acceptable on the piezo and where the actual risk sits.** The quadrant wires are driven
by a low-impedance amplifier output — 100 MOhm of leakage there is nothing. But the disc is where
the **tip holder** mounts, on a sapphire insulator, and the tip is the input node. **100 MOhm from a
quadrant at 10 V across to the tip injects 100 nA** — the same order as the 119 nA already blocking
this build, and roughly a hundred times a tunneling current.

**So the mitigation is build order, not chemistry:** solder and clean all four quadrant wires
first, then build the sapphire, tip holder and tip on top of a board that is already clean. **Never
the other way round.** Use the least paste that will make the joint.

### 10.7 Not done, and honest about it

- **We do not know whether we already own this paste, or how many piezo discs are in the room.**
  Recorded as gaps in `docs/INVENTORY.md` per `CLAUDE.md` §3d, and asked rather than inferred.
- **The price is unverified** — the two sites that would confirm it are blocked from this session.
- The 138 C and 210 C figures are now canonical in `docs/FACTS.md`, but they are **still restated in
  prose** in `docs/START_HERE_gotchas.md` and `docs/OTHER_BUILDERS.md`. They agree today. **They are
  the kind of copy that drifts** — worth converting to links next time either file is touched.

---

## 11. The flux, in a syringe rather than a jar — added later the same day

**Jacob's actual question**, which section 10 answered wrongly: not the piezo solder, **the flux for
the preamp rebuild**, and whether it comes in a syringe rather than a jar. Documentation only.

### 11.1 The answer is no, and that is a finding rather than a dead end

**Chip Quik's RMA791 is jar-only.** Its whole paste-flux family — RA691, RMA771, RMA791, RA891 — is
2 oz flat jars. No syringe SKU exists. **`docs/PREAMP_SHOPPING_LIST.md` said "50 g jar" because
there was no alternative, not because a jar was preferred**, and its own item line still asked for a
"gel syringe". Those two disagreed and nobody had noticed.

### 11.2 Why the obvious substitution is not safe

Chip Quik's syringe products are a **different line — "tack flux"** — and their J-STD-004 codes and
their own vendor descriptions **contradict each other about the chemistry.**

In J-STD-004 the first two letters are the chemistry: **`RO` is rosin, `RE` is synthetic resin.** So
the code should settle our one criterion outright. **It does not.**

- **SMD491-10M** is classified **ROL0** — and TestEquity's own product title is "Tack Flux,
  **Synthetic** No-Clean, Halogen-Free, ROL0."
- **NC191** is **ROL0** and is described in the same breath as a "synthetic no-clean flux".
- **RMA591NL** is **ROM1**, has "RMA" in its name, and is described as "low viscosity RMA
  **synthetic** paste".
- **SMD291**, the most widely stocked of them, is **REL0** — **`RE`, so not rosin at all** — despite
  a composition list that mentions rosin.

**This is the same trap that produced the flux we already own**, which is a non-rosin no-clean.
Buying a syringe off the code alone would repeat it.

### 11.3 What could not be verified, and why that is the whole point

**chipquik.com, mouser.com and amazon.com are all blocked by this session's network egress proxy.
Not one primary datasheet could be opened.** Every classification above comes from a search snippet.

**So no recommendation was made among those four.** `CLAUDE.md` §3c: if the number cannot be got,
say so and give a route that does not depend on it.

### 11.4 The route that does not depend on it

**Keep the RMA791 jar. Buy empty 10 cc Luer-lock syringes and load them from it.** About $10 for
ten. The chemistry stays the one already reasoned through — **ROL0: rosin, low activity,
halide-free**, the mildest of everything surveyed. Back-load the barrel with the plunger removed;
paste will not draw up through a tip.

**A better argument for the syringe than the one already written down.** The shopping list justified
a syringe only as "less awkward for surface-mount rework". The real reason on this board is
different: **an open jar that a brush is repeatedly dipped into becomes a contamination reservoir** —
skin oils, board debris, spent flux, all returned to the supply. Given that a human body within a
metre injects 20-50 nA into this input node, re-dipping into a shared jar is not a small thing. A
syringe dispenses a fresh bead and the bulk is never touched.

**Single-purchase alternative, with its cost stated:** **MG Chemicals 8342**, 10 mL / 9.5 g syringe.
The only syringe flux found whose **manufacturer's product name** says rosin — "RA Rosin Flux Paste"
— rather than only a code. Its TDS says residue removal is **required** and that IPA removes it,
which matches our protocol better than any no-clean does. **But it is RA, not RMA:** ROM1, with
**0.5-2.0% halides by weight.** §10.2 of Nuh's log chose RMA over RA specifically because the milder
activator fails gracefully if a trace survives. **Halides do not fail gracefully**, so with this one
the distilled-water rinse becomes the step that carries the risk rather than a refinement.

### 11.5 Still open

**The flux pen.** The shopping list asked for "gel syringe **and a pen**" and only the paste was ever
chosen. **Most flux pens are low-solids no-clean — the wrong chemistry.** No rosin pen has been
verified. Recorded rather than guessed.

### 11.6 Process note

**Section 10 of this log answers a question that was not asked.** "Solder" was read literally when
the context — a rebuild whose entire consumables list is about flux and cleaning — pointed at the
flux. The section is kept because its content is independently correct and it closed a real open
question, but **the misread cost a round trip.** Where a request is ambiguous and the repository
makes one reading far more likely, say which reading is being taken before doing the work.

---

## 12. Purchases made, and the piezo reported built — added later the same day

**Jacob ordered four items (screenshot, arriving 2026-09-11) and stated the piezo scanner is
finished.** `CLAUDE.md` §3d: an order confirmation outranks every document here, and what he tells us
has to reach `docs/INVENTORY.md` in the same session or it is lost at the next compaction.

### 12.1 What was bought

| Item | Why it is right |
|---|---|
| **Chip Quik RMA791 rosin paste flux, 50 g jar** | The flux chosen in Nuh's §10.2. **ROL0 — rosin, low activity, halide-free.** The mildest option surveyed |
| **Manual flux/glue syringe dispenser**, 10 cc syringe + blunt tips | **Solves the jar-vs-syringe question by buying both halves.** Load the syringe from the jar |
| **M2 pan-head self-tapping screws, 800 pc, M2x4 to M2x20** | Board to box standoffs. Replaces the superglue that is the leading suspect for the 119 nA |
| **Heat shrink tubing, assorted kit** | The five JP1 wire joints |

**Two of `docs/INVENTORY.md`'s known gaps close** — M2 screws and heat shrink. **Copper tape is now
the only unbought item** in the "buy only if we do not have it" table.

### 12.2 M2x6, not M2x8 — measured rather than assumed

The assortment spans M2x4 to M2x20, so the length has to be chosen. **Measured from the mesh with
`Code/pc/stl_features.py` rather than guessed**, per `CLAUDE.md` §3b:

```
1_preamp_box_base.stl
   5.63  14.83   6.000   Z 0.00..4.00     <- boss, 4.00 mm tall
   5.63  14.83   1.600   Z -1.00..4.00    <- pilot, 5.00 mm deep, blind
   5.63   3.40   6.000   Z 0.00..4.00
   5.63   3.40   1.600   Z -1.00..4.00
```

Fit error 0.0001. The pilot is **blind at 5.00 mm**, with 1 mm of floor left beneath it.

| Screw | Engagement through a 1.6 mm board | Verdict |
|---|---|---|
| M2x4 | 2.4 mm | Works. Thin, but the board weighs nothing |
| **M2x6** | **4.4 mm into a 5.0 mm hole** | **Use this. 0.6 mm to spare** |
| M2x8 | **6.4 mm needed, 5.0 mm available** | **Bottoms out by 1.4 mm.** Cracks the Ø6 boss or jacks the board off the standoff |

**The 1.6 mm board thickness is an assumption — JLCPCB's default, never measured.** At 1.0 mm an
M2x6 would engage exactly 5.0 mm and bottom out too. **Put calipers on the board; it is a ten-second
check and we own calipers.** `CAD/prints/README.md` already says drive slowly and stop at snug —
the thread is plastic, there is no metal insert.

### 12.3 The flux is bought. The thing that makes it safe is not.

**None of 99% IPA, distilled water, the four soft brushes, lint-free wipes or foam swabs was in this
order.** RMA791 is rosin and therefore removable — **but only if something removes it.** Rosin left
on this board is a leakage path at the precise node the whole project is blocked on.

**Recorded in the shopping list as a blocking note: do not solder the preamp until the cleaning kit
is in the room.** Buying flux without the means to clean it off makes the board worse, not better.

### 12.4 The piezo is built — recorded as stated, not as understood

**`SAID` only.** Nothing about how it was done has been stated and **none of it is inferable from any
file** — `CLAUDE.md` §3d is exactly this case. Written into `docs/INVENTORY.md` as four explicit
UNKNOWNs rather than filled in:

- **How the quadrant wires were attached** — solder and which alloy, or conductive epoxy
- **Whether the disc still actuates.** A depolarised disc still meters correctly. This is not a
  resistance check
- **Which disc was used** — `docs/FACTS.md` carries an unresolved Ø20.500 mm seat against a
  25-27 mm disc
- **Whether the four quadrant wires were labelled.** The time-critical one

**Consequences for the docs:** the Sn42/Bi58 purchase in §10 is **moot — do not buy it**, and the
iron-temperature question closed in §10.5 is moot for this build. Both are struck through in
`docs/BOM.md` and `docs/OPEN_QUESTIONS.md` rather than deleted, because the reasoning still applies
to any future disc. **§10 is left standing in this log; session logs are history.**

### 12.5 The one question worth asking before Friday

**Were the four quadrant wires labelled as they were soldered?** They are identical bare enamelled
copper with no colour code and the mapping is not recoverable from any file or photo.

**If they were not, nothing is actually lost** — a rotated or mirrored first image identifies them
and it is fixable in software. **The point of asking is to stop a future session spending bench time
hunting for a record that was never made.** Answer either way and it goes in the file.

---

## 13. Session close

**Documentation only. Nothing was powered. No measurement was taken on hardware.** The one number
produced this session came out of a mesh, not a meter.

### 13.1 The piezo scanner is built and working — closed

**`SAID` 2026-09-09, Jacob: finished, and it works.** Recorded as fact in `docs/INVENTORY.md`.
**Three items close with it**, and none should take bench time again:

| Item | Now |
|---|---|
| Sn42/Bi58 low-temp paste (§10) | **Do not buy.** The joints are made |
| Iron temperature and dwell (§10.5) | **Moot.** Reasoning kept for any future disc only |
| Ø20.500 mm seat vs a 25-27 mm disc | **Closed by construction.** A working scanner exists, so whatever went in fits. It was a documentation artefact |

**Quadrant-to-axis mapping was not recorded and does not need to be.** It falls out of the first
image and is fixed in software. **Struck from the questions worth asking.**

### 13.2 The only measurement this session

`Code/pc/stl_features.py` on `1_preamp_box_base.stl`, fit error 0.0001:

| Feature | Value |
|---|---|
| Standoff boss | **Ø6.000, 4.00 mm tall** |
| M2 pilot | **5.00 mm deep, blind**, 1 mm of floor beneath |
| **Screw to use** | **M2x6.** M2x8 needs 6.4 mm through a 1.6 mm board and **bottoms out by 1.4 mm** |

**Board thickness is assumed, not measured** — 1.6 mm is JLCPCB's default. At 1.0 mm an M2x6 also
bottoms out. **Calipers on the board before driving anything.**

### 13.3 Corrections to earlier documents

| Where | It said | Now |
|---|---|---|
| `docs/PREAMP_SHOPPING_LIST.md` | Item line asked for a **"gel syringe"**; the chosen product was a **50 g jar** | **The two contradicted each other and nobody had noticed.** RMA791 is jar-only — no syringe SKU exists in Chip Quik's paste-flux family. Settled by buying the jar **and** a dispenser |
| `docs/PREAMP_SHOPPING_LIST.md` | "Do not buy: **bismuth/low-temperature solder**", unscoped | **Scoped to the preamp board.** Read cold it contradicted `docs/BOM.md` §5. Now moot anyway — the piezo is done |
| `docs/PREAMP_SHOPPING_LIST.md` | "Do not buy: **solder of any kind**" | **"Solder wire of any kind."** As written it forbade the piezo paste the BOM asked for |
| `docs/BOM.md` §5 | Low-temp paste, "1 syringe", to buy | **Struck through. Do not buy — the piezo is built** |
| `docs/FACTS.md` | Ø20.500 seat vs 25-27 mm disc: **CONFLICT** | **Closed by construction, 2026-09-09** |
| `docs/OPEN_QUESTIONS.md` §4 | Piezo iron temperature "undocumented" | **Moot — the piezo is built.** Derivation kept for a future disc |
| `docs/INVENTORY.md` | M2 screws / heat shrink "nobody has checked" | **Both ordered.** Copper tape is now the only unbought item in that table |

**Nothing here contradicts a measurement.** Every correction above is a scope or a stale-intent
problem, not a wrong number.

### 13.4 What was ruled out

- **Substituting a syringe flux for the jar.** Four candidates rejected: Chip Quik SMD491 and NC191
  are classified `ROL0` but titled "**Synthetic**" by their own vendors; RMA591NL is `ROM1` and
  called "RMA **synthetic**"; SMD291 is **`REL0`** — `RE` is resin, **not rosin**. **The J-STD-004
  code alone does not settle the chemistry**, which is precisely how the wrong flux already in the
  drawer got bought. **No primary datasheet could be opened** — chipquik.com, mouser.com and
  amazon.com are all blocked by this session's egress proxy.
- **M2x8 for the preamp box.** Bottoms out. Ruled out by measurement, not by feel.

### 13.5 The one thing standing in the way

**The cleaning supplies were not in Friday's order.** No 99% IPA, distilled water, soft brushes,
lint-free wipes or foam swabs. **RMA791 is removable only if something removes it, and rosin left on
this board is a leakage path at the exact node the project is blocked on.**

**Do not solder the preamp until the cleaning kit is in the room.** In `STATUS.md`.

**Copper tape** for the shield is the other outstanding purchase.

---

## 14. Audit of the audit: three defects in the instruction files themselves

**Jacob's report: "a lot of idiocy from the model that wasn't there till recently", and a suspicion
that the last audit caused it.** Investigated. **He is right that something changed, and the
mechanism is in the instruction files rather than in any measurement.**

**`CLAUDE.md` grew from 208 lines on 2026-09-05 to 403 on 2026-09-09** — it nearly doubled in four
days. The 2026-09-08 architecture audit **did flag the accretion**, and that recommendation was
**reversed during implementation** (`sessions/2026-09-08.md`, "Two audit recommendations revised").
**The reversal was right about merging and wrong about the consequences**, because it only
considered §3/3b/3bb/3c. Nobody re-examined §4 or §3bb's wording.

### 14.1 Defect 1 — two safety-rule citations pointed at the wrong rule

`STATUS.md` cited **"safety rule 10"** twice meaning **the DAC modulo-wrap hazard**. Rules were
later inserted above it and that content became **rule 13**, while **rule 10 became "do not glue the
spare preamp board"**. The citations were never updated.

| Line | Said | Reads as | Actually meant |
|---|---|---|---|
| `STATUS.md:153` | "safety rule 10 (an out-of-range DAC command silently jumps to the opposite rail)" | do not glue the spare board | **rule 13** |
| `STATUS.md:986` | "See safety rule 10 — this is a tip hazard" | do not glue the spare board | **rule 13** |

**A session following either one reads a rule about adhesive and never learns that a mistyped DAC
value silently slams an axis to the opposite rail.** That is a tip hazard. **Both corrected.**

### 14.2 Defect 2 — two different safety lists, with clashing numbers

**`CLAUDE.md` §4 was a numbered list of seven. `STATUS.md` has sixteen — 0, 0b, 0c, 1 to 13.** And
`CLAUDE.md` opened with **"These are in `STATUS.md` too"**, asserting they were the same list.

| Number | `CLAUDE.md` §4 meant | `STATUS.md` means |
|---|---|---|
| 4 | Park Z at midscale | ~~JP1 wire rule~~, retired |
| 6 | Nobody within a metre | Park Z at midscale |
| **7** | **Never send `CCON`** | **Meter the tip holder against the brass electrode** |

**`CLAUDE.md` is in context at all times; `STATUS.md` is read once at session start.** So the
always-present list was shorter, differently numbered, and asserted to be identical — and
`STATUS.md` fault E cites "safety rule 7" for the glue path, which resolves against `CLAUDE.md` to
the `CCON` rule.

**`CLAUDE.md` §4 also silently omitted four hazards**, including **the DAC modulo-wrap** (rule 13),
**the 45-minute warm-up** before any preamp measurement is valid (rule 0), and **"a multimeter
cannot clear a leakage path — OL only proves >60 MΩ"** (rule 12).

**Fixed by removing the numbering from `CLAUDE.md` §4 entirely** and making it an explicitly
unnumbered summary that names `STATUS.md` as the only citable list, plus the four missing hazards.
**Nothing cites "`CLAUDE.md` §4 rule N"** — checked — so no reference broke. The audit's objection
to renumbering applied to §3/3b/3bb/3c, which **are** cited from ten places and were **left alone**.

### 14.3 Defect 3 — a rule the repository has never followed and cannot

`CLAUDE.md` §3bb said: **"Do not restate one of those numbers in another document. Link to it."**
§6's ownership table said **"link to it, never restate"**.

**Measured:**

| Value | Live documents containing it |
|---|---|
| `10.24 V` | **11** |
| `119 nA` | **10 — including `CLAUDE.md` itself** |
| `0.3125` | 5 |
| `102.4 nA` | 4 |

**The 2026-09-08 audit had already decided against this rule in writing** — its "One honest
limitation" section concluded that a competing *register* is the failure mode, that a number *cited
in an argument* is how prose works, and that **the protection is the checker, not a prohibition.**
**`CLAUDE.md` was never updated to match that decision.**

**This is the most corrosive of the three.** A rule that every document breaks, including the file
stating it, **teaches the reader that the rules here are decorative.** Once one absolute is
discovered to be theatre, the neighbouring absolutes — the ones that are real, like "never `APRH`"
— lose force too. **Rewritten to say what the audit actually decided: no second register; prose
citations are fine and machine-guarded.**

### 14.4 The fix that stops Defect 1 recurring — and its honest limit

`check_facts.py` gains `check_safety_rule_refs()`. **The audit's own lesson was "a checker beats a
rule", so this is a checker.**

**The first version was not good enough and is worth recording.** It verified that a cited rule
number *exists*. **That would not have caught this bug** — rule 10 existed, it just meant something
else. Confirmed by re-introducing the fault: the first version reported clean.

**The version committed compares vocabulary.** For each "safety rule N" citation it takes the
content words of the citing sentence and of rule N's own text; **no overlap at all means the
citation is pointing at the wrong number.** Verified both ways:

| Test | Result |
|---|---|
| Both original miscitations restored | **Both flagged**, exit 1 |
| A citation to a nonexistent rule 99 | **Flagged** as missing |
| Repository as corrected | **Clean**, exit 0 |
| `stm_approach_test.py` | **40 tests pass** |

**Its limit, stated plainly:** it is a vocabulary heuristic, not comprehension. A citation whose
sentence happens to share a common word with the wrong rule will pass. Citations with fewer than
three content words — "see safety rule 10" and nothing else — are skipped as having too little to
judge. **It catches the drift pattern that actually occurred; it is not a proof of correctness.**

### 14.5 What this says about the cause

**The audit did not cause the problem. It half-fixed one and left two adjacent ones untouched**,
and the growth continued afterwards — §3d and this section were both added after it ran.

**The pattern in all three defects is the same: a rule and its referent drifted apart, and nothing
checked.** `STATUS.md`'s numbering moved under its own citations; `CLAUDE.md` §4 drifted from the
list it claimed to copy; §3bb drifted from the decision that superseded it. **In a repository whose
whole method is "one canonical home per fact", the instruction files were the one place that method
had never been applied to.**

**Not changed, deliberately:** §3/3b/3bb/3c/3d numbering, cited from ten documents. **The problem
was never that the rules are numbered oddly — it is that some of them were not true.**

---

## 15. Deeper pass: the start-up hook was feeding every session bad information

**Jacob, on reading §14: "go deeper, try harder."** He was right to push. §14 audited the documents
a session is *told* to read. **It never looked at the thing a session actually reads first —
`.claude/session-start.sh`, whose output lands in context before any file is opened.**

**Four defects, all live, all firing every session.**

### 15.1 It named the wrong session log — the worst of the four

```bash
LATEST=$(ls -1 sessions/2*.md 2>/dev/null | sort | tail -1)
```

`sort` here is **lexical**, and `-` (0x2D) sorts before `.` (0x2E). So:

```
sessions/2026-09-09-jacob.md      <- sorts FIRST
sessions/2026-09-09.md            <- sorts second, so tail -1 picks THIS
```

**Any `YYYY-MM-DD-name.md` sorts before the plain `YYYY-MM-DD.md` of the same day.** So when two
people log a date — which is the normal case here, and happened today — **the hook always names the
first log and never the second.**

`CLAUDE.md` §2 tells every session to read "the newest file in `sessions/`". **The hook implemented
that instruction and got it backwards.** Since 2026-09-09 every session has been pointed at Nuh's
morning log while Jacob's later one, containing that day's corrections, went unmentioned.

**This is the strongest single explanation for "the model does not know what happened recently".**
It is not a subtle prompting effect. The session was handed the wrong file by name.

**Fixed in both places.** The hook now takes the newest **date**, then lists **every** log carrying
it and says how many there are. `CLAUDE.md` §2 no longer says "file", singular.

### 15.2 It reported one cause for four different failures — and for crashes

```bash
FACTS_OUT=$(python3 Code/pc/check_facts.py 2>/dev/null)
if [ $? -ne 0 ]; then
    echo "$FACTS_OUT" | head -30
    echo "ACTION FOR CLAUDE: a value docs/FACTS.md lists as RETIRED is still in a live document."
```

`check_facts.py` reports **four** distinct problems — retired values, broken file references,
archived documents cited as current, and now wrong safety-rule citations. **The hook announced a
retired value every time.** Three times in four it named the wrong cause and sent the session
hunting the wrong thing.

**And `2>/dev/null` discarded stderr.** A crashed checker therefore produced an **empty** report
followed by that same confident wrong diagnosis. **A session would have been told there was a stale
number in a document when in fact nothing was checking any number at all.** I proved this by
breaking the script deliberately; the new hook prints the traceback and says
"**BROKEN CHECKER, not a documentation problem.**"

### 15.3 It was hardcoded to `main` and would merge into a branch unasked

`BRANCH="main"` was a constant, not the checked-out branch. On any feature branch the hook reported
that branch's distance from `main` as if it were its own, and the auto-fast-forward arm would have
run **`git merge --ff-only origin/main` into that branch** with nobody asking. Now it reads the real
branch and, off `main`, **reports only — it never merges.**

### 15.4 It printed a STATUS.md header cut off mid-sentence

```bash
sed -n '3,4p' STATUS.md
```

A **positional** slice of the one file `CLAUDE.md` §5 requires rewriting at the end of **every**
session. It had already drifted — this session opened with:

```
**Updated by:** Jacob, remote. **Documentation only — nothing was powered or measured.** C2's
```

**Ending on "C2's".** The single most prominent line of state a session receives, truncated
mid-clause. Now matched by label rather than line number.

### 15.5 My own error, in the same class

**I pushed commit `1198651` to `main` with `check_facts.py` exiting 1 on its own repository.** The
`&&` chain I used tested the wrong thing, so the failure went past me. **That is exactly the defect
§14 documents — a check that exists and is not believed** — committed by the session writing it up.
Fixed in this commit, and it is why §15.2's crash-versus-findings distinction matters: a broken
checker must be loud.

### 15.6 The first regex fix was wrong, and that is worth recording

§14.4's checker skips lines that are *discussing* a miscitation. The first attempt reused the
existing `EXCUSES` regex over a ±2-line window. **That regex matches `wrong` and `error`** — both
of which sit within two lines of both real miscitations, in ordinary technical prose
("...numbers in the reference were **wrong**", "...with no **error**").

**Result: the check went green while the bug was present.** Caught only because the regression test
re-introduced the fault and expected a failure. **A test that only ever runs against a clean tree
proves nothing.** Replaced with a narrow `CITATION_TALK` pattern.

| Regression test | Result |
|---|---|
| Corrected repository | clean, exit 0 |
| Both original miscitations restored | **both flagged**, exit 1 |
| Citation to a nonexistent rule 99 | **flagged** as missing |
| Hook with a deliberately crashed checker | **"BROKEN CHECKER"**, traceback shown |
| Hook with a real finding | **checker's own output** printed |
| Hook on a feature branch | **reports only, no merge** |
| `stm_approach_test.py` | 40 pass |

### 15.7 What actually degraded, in one line

**Nothing in this repository's engineering was wrong. Its self-description was.** The start-up
hook, `CLAUDE.md` §4, §3bb, §2, and two `STATUS.md` citations had all drifted from what they
describe — **and every one of them drifted in the direction of sounding authoritative while being
wrong**, which is worse than being absent. A session cannot detect that from inside.

**The repository's own method — one canonical home per fact, and a checker rather than a rule —
had never been applied to the files that state the method.**

---

## 16. Third pass, systematic: the root cause was a naming convention nobody agreed

**Jacob, after §15: "spend 45 minutes... your entire point is to never be shallow... audit yourself
and the model. This was very concerning today."**

**He is right to be concerned, and the third pass found more than the first two combined.** §14 and
§15 each followed one thread to a tidy conclusion and stopped. This pass **enumerated every
mechanism that can mislead a session first, then tested each** — and the first thing that fell out
was that **§15's own fix was incomplete.**

### 16.1 The root cause, which is one level below everything in §14 and §15

**`sessions/README.md` states the convention as "one file per work session, named
`YYYY-MM-DD.md`". Four logs have broken it since the very first session:**

```
sessions/2026-08-31-plan.md
sessions/2026-08-31-results.md
sessions/2026-09-06-plan.md
sessions/2026-09-09-jacob.md
```

**The convention was never true.** And because every mechanism that consumes session logs was
written against the singular form, **a second log for a date fell through all of them.** §15's
lexical-sort bug in the hook was not the cause — **it was one symptom of eight.**

| # | Where | What it assumed |
|---|---|---|
| 1 | `.claude/session-start.sh` | `ls \| sort \| tail -1` — **fixed in §15** |
| 2 | `CLAUDE.md` §2, read at start | "the newest **file**" — **fixed in §15** |
| 3 | `CLAUDE.md` §3, the precedence list | "the newest file in `sessions/`" |
| 4 | `CLAUDE.md` §5 step 1, writing the log | "if a file for today already exists, **add to it**" |
| 5 | `CLAUDE.md` §6, the ownership table | `sessions/YYYY-MM-DD.md` |
| 6 | `.claude/commands/catchup.md` | "the newest file in `sessions/`" |
| 7 | `.claude/commands/wrap.md` | "add to it rather than overwriting" |
| 8 | `sessions/TEMPLATE.md` | "copy this to `sessions/YYYY-MM-DD.md`" |

**§15 fixed two of eight and reported the problem solved.** That is the same shallowness Jacob is
complaining about, occurring **inside the fix for it**.

### 16.2 The consequence that had already happened

**`sessions/2026-09-09.md` — Nuh's whole day's work — was missing from `sessions/README.md`.**

So today's log was invisible to **both** discovery mechanisms at once: the hook's sort skipped it,
and the index never listed it. **A session had no way to find Nuh's work except by listing the
directory.** Two independent blind spots, the same file, from the same root cause.

### 16.3 The one that would have corrupted the other person's log

**`/wrap` step 1 and `CLAUDE.md` §5 both said: "if a file for today already exists, add to it."**

Today the existing file was **Nuh's**. **Following that instruction literally writes Jacob's session
into Nuh's log** — in a directory whose own rule is "append-only, never rewritten". It was avoided
by judgement, not by the instruction. **Both now say: check whose log it is, and if it is not yours,
start `YYYY-MM-DD-<your name>.md`.**

### 16.4 `/wrap` also said to delete history

> "If a fault was fixed, **remove it**. If a rule no longer applies, **remove it**."

**That contradicts the convention this repository actually runs on** — struck-through entries with
banners, kept: safety rule 4, fault 4a, the archive. **Twice in this project a retraction has itself
turned out to be wrong** (fault 5, and A.5.8's retraction). **A deleted rule leaves nothing to check
that against.** Changed to strike-through-and-say-what-replaced-it.

### 16.5 The checker was not checking two live files

`check_facts.py` skipped the whole `sessions/` directory as history. **But `sessions/README.md` is a
live index and `sessions/TEMPLATE.md` is copied into every future log** — neither is history.

**Turning the scan on immediately found three retired values that had been sitting there:**

| File | Value | Now |
|---|---|---|
| `sessions/README.md` line 24 | `37 nA` | marked superseded, ~119 nA |
| `sessions/README.md` line 25 | `37 nA` | marked superseded, ~119 nA |
| `sessions/README.md` line 35 | `37 nA` | marked superseded, and the shield noted as eliminated |

**A reader scanning the index for which log to open saw "37 nA leakage" three times as a current
fact.** This is precisely the failure `docs/FACTS.md` exists to prevent, hidden in the one directory
the checker was told to ignore.

### 16.6 What was checked and found sound

**Recorded so the next audit does not repeat it.**

| Checked | Result |
|---|---|
| All 32 `fault N` citations across live docs | **All resolve.** Including the `fault 0a` alias, defined inline at fault 5 |
| Retired values hardcoded in `Code/` | **None.** `stm_approach.py`'s `4.096` mentions are correction comments, not defaults |
| `docs/` and root `.md` files | Already scanned by the checker; clean |
| Firmware constants | No retired values |

**Two cosmetic things left alone:** `STATUS.md`'s fault list runs `1, 1c, 1b, 1d` — out of order —
and `fault 0a` is an alias with no heading of its own. **Both resolve correctly and renaming them
would break citations across ten documents.** Noted, not changed.

### 16.7 Self-audit: why this took three passes

**The generative fault was not effort. It was the stopping rule.**

| Pass | Stopped because | Missed |
|---|---|---|
| §14 | Three defects made a coherent story | The hook, which every session reads first |
| §15 | A root cause was identified and it felt complete | Six of the eight instances of that same root cause |
| §16 | — | (enumerated first, then tested) |

**Each time, a satisfying causal narrative was mistaken for an exhausted search.** In both earlier
passes a one-line `grep` would have shown the rest immediately. **The failure was declaring
completion, not failing to look.**

**Three more errors of mine today, in the same family:**

- **Commit `1198651` was pushed to `main` with `check_facts.py` failing on its own repository.** The
  `&&` chain used to gate it reported the success of the wrong command.
- **The first version of the safety-rule checker passed while the bug was present.** It reused a
  regex matching `wrong` and `error` — both present within two lines of both real miscitations. It
  had only ever been run against a clean tree, so it looked green.
- **"Solder" was read literally** when the surrounding work — a rebuild whose entire consumables
  list is flux and cleaning — made "flux" far more likely. §12.6.

**Four rules added to `CLAUDE.md` §7** covering exactly these: grep for the whole class before
declaring a fix, re-introduce a fault to prove a test can fail, run the checker as its own command
and read its exit code, and treat "I found the root cause" as a hypothesis rather than a stopping
condition.

### 16.8 What was NOT the cause — correcting my own earlier framing

**`CLAUDE.md` doubling in length looked like the obvious culprit and it is not.** No defect found
across all three passes came from length. **Every one came from a statement that had become
untrue** — a citation pointing at a renumbered rule, a list claiming to mirror another list, a
naming convention nobody followed, a hook reporting a cause it had not determined.

**Length was a red herring. Accuracy was the problem.** Written into `CLAUDE.md` §7 so nobody
compresses the file believing it will help.

### 16.9 Regression battery — every check proved able to fail

**No check is trusted here unless the fault has been re-introduced and seen to trip it.**

| Test | Result |
|---|---|
| Clean repository | exit 0 |
| Unindexed session log | **detected** |
| Retired value in `sessions/TEMPLATE.md` | **detected** (was invisible) |
| Retired value in `sessions/README.md` | **detected** (was invisible) |
| Broken file reference | **detected** |
| Both original `safety rule 10` miscitations | **detected** |
| Citation to a nonexistent rule 99 | **detected** |
| Hook with a crashed checker | **"BROKEN CHECKER"**, traceback shown |
| Hook with two logs for one date | **both listed, count stated** |
| Hook on a feature branch | **reports only, refuses to merge** |
| `stm_approach_test.py` | **40 pass** |
| `bash -n` on the hook | clean |

**9 of 9 checker assertions passed, both hook behaviours confirmed, 40 unit tests pass.**

---

## 17. Why the quality dropped — the diagnosis that was actually tested

**Jacob asked a third time, and added that Nuh had reported the same thing.** That second fact is
what broke the case open, because it rules out everything specific to one session.

**§14, §15 and §16 fixed real defects and then answered the wrong question.** They asked "what is
wrong with the repository". Jacob asked "why did the answers get worse". **Those are not the same
question, and I never tested whether the first answered the second. It does not.**

### 17.1 The mis-attribution, stated plainly

**Not one of the defects found in §14–§16 explains any observed failure.**

The clearest case is the one that started this. Jacob asked for "the solder to buy, syringe not a
bucket" and got an answer about piezo solder paste.

- The start-up hook named `sessions/2026-09-09.md`. **That is Nuh's log, and it is the flux one** —
  it mentions flux five times and contains the whole flux decision. **The hook pointed at the right
  file.**
- `docs/PREAMP_SHOPPING_LIST.md`, at the commit current when the session began, has as **the first
  row of its Buy table**: *"Rosin flux, gel syringe + a pen"*. **I printed that file in full in my
  own output before answering.**

**The correct answer was on screen, in the first row of a table I had just displayed.** No missing
file, no wrong pointer, no stale number. **The repository was not the cause.** §16 even
congratulated itself for enumerating mechanisms first — and still never checked its conclusion
against the symptom it was supposed to explain.

### 17.2 What the evidence actually rules out

| Hypothesis | Evidence | Verdict |
|---|---|---|
| **The model changed** | `get_session`: `configured_model` and `last_served_model` both `claude-opus-5`, `effort_level: high`, no fallback. **Checked twice, on two days** — §8 and again today | **Ruled out** |
| **Context exhaustion** | This session: **317,870 of 1,000,000 tokens**. The solder/flux misread happened in its **first exchange**, at minimal context | **Ruled out for this session** |
| **Repository defects** | §17.1 — the correct information was visible in every case | **Ruled out as the cause** (the defects were still worth fixing) |
| **CLAUDE.md length** | No defect found in three passes came from length | **Ruled out**, §16.8 |

### 17.3 The mechanism, and it is one thing

**Eight errors, three sessions, both people's work, one shape: act on the first plausible reading,
write it in as fact, skip the check that would have disconfirmed it.**

| Session | Error | The premise never checked |
|---|---|---|
| Nuh, 09-09 | `OPA124U` written into **five documents** as our fitted part | **Whose board is in the photograph?** It was Berard's |
| Nuh, 09-09 | 11301 hole Ø2.03 mm, "drops into our hole" | **Is 2.03 a diameter?** It is the length below the flange. Real fit is interference |
| Nuh, 09-09 | Flux rule: "must NOT say no-clean" | **Do no-clean and rosin describe the same axis?** They do not |
| Jacob, 09-09 | Preamp was hand-populated | **Was it ordered as a PCBA?** It was |
| Jacob, 09-09 | We own the Keystone 11301 | **Which part number is on the order?** The 11311 |
| Jacob, 09-09 | Nobody checked C1/C2 polarity | **Did the fab raise it?** JLCPCB did, at order time |
| This session | "Solder" meant piezo paste | **Which row of the buy list?** Row 1: rosin flux |
| This session | Pushed a failing checker to `main` | **Did the check actually pass?** It exited 1 |
| This session | "Root cause found", twice | **Does this cause explain the symptom?** Neither did |

**Nine, not eight.** Every one is the same move. **Not a knowledge failure — a verification
failure.** In each case the disconfirming check was cheap, available, and skipped, and the answer
was then stated with the confidence of something checked.

**This is why it reads as "dumber".** A wrong answer that is hedged reads as uncertainty. A wrong
answer delivered with the cadence of a verified one reads as incompetence — and it is worse than
uncertainty, because it cannot be caught from the outside.

### 17.4 Why it feels recent — a hypothesis, marked as one

**Up to about 2026-09-05 the questions were derivable**: firmware behaviour, netlists, mesh
geometry, datasheet limits. The repository can answer those, and the answers were good.

**From 2026-09-06 the questions changed category** — what arrived in the post, what a photograph
shows, how a board was assembled, what a supplier asked, **what Jacob meant by "solder"**. The
repository cannot answer any of those. **Every one of the nine failures above is in the new
category.**

**So the error rate on derivable questions may not have changed at all. The proportion of questions
that are not derivable went up sharply, and that is the category this failure mode attacks.**

**Marked as a hypothesis.** It fits all nine and I cannot measure it from inside the sessions.
**What would disconfirm it:** a run of wrong answers on pure derivation — a netlist, a mesh, a
firmware trace. If that happens, this explanation is wrong and should be replaced, not defended.

### 17.5 Why another written rule would have been worthless

**The rule already existed, four times over, and was violated anyway:**

- §1: *"Tell us when you are unsure... marking things UNKNOWN or VERIFY rather than guessing."*
- §3d: *"If you cannot ask, write UNKNOWN. Never fill the gap by inferring from a design document."*
- §7: *"Measurements over inference. If a number was measured, say so."*
- §7, added yesterday: four rules about verifying before claiming.

**A fifth restatement would have been the exact anti-pattern this repository documented on
2026-09-09: a rule that everything breaks teaches the reader that the rules here are decorative.**
So this session added **mechanism, not prose**.

### 17.6 What was actually built

**1. A commit is now refused while `check_facts.py` fails.** `.githooks/pre-commit`, installed
automatically by `.claude/session-start.sh` via `core.hooksPath` — **so it reaches Nuh's computer
with nobody running anything.** This makes the failing-checker push of commit `1198651`
*impossible* rather than discouraged. `git commit --no-verify` remains for a deliberate mid-fix
commit.

**2. The checker now detects its own blind spots.** `check_dead_qualifiers()` reports any RETIRED
row whose qualifier matches no wording in any document. **This generalises a finding Nuh recorded
in `sessions/2026-09-09.md` §9 and nobody acted on:** the `5.93 mm` row was qualified as *"the
preamp mounting-hole spacing"*, words appearing in no document, so **the checker reported clean
while three live files carried the wrong number.** Nuh fixed that one row by hand and wrote:
*"A silent checker is worse than no checker, because it is believed... confirm it fires before
trusting it."* That confirmation is now automatic. **Verified by restoring Nuh's exact original
wording: the check fires and names the row.**

**3. Nuh's other recommendation, applied.** `sessions/2026-09-09.md` §9 said the photograph rule in
`CLAUDE.md` §7 "should probably say" it generalises beyond wire colours. **It was written down on
09-09 and never applied.** It now covers any hardware fact, alongside a second rule from the same
section: **a search-result summary is not a datasheet.**

**4. The mis-attribution corrected in `STATUS.md`**, where it had been recorded as fact. Leaving a
wrong causal claim in the live-state file would have been the same defect §14 was written to
remove.

### 17.7 The honest limit

**Items 1 and 2 are mechanisms and will hold.** Item 3 is prose and carries the same weakness as
every other rule here.

**Nothing built today prevents the core failure directly.** No hook can ask "whose board is in that
photograph" or "which row of the buy list did he mean". **The only real protection is the one this
project already relies on: Jacob and Nuh saying "that's wrong" — and it has worked every single
time.** All nine errors were caught. Three times by Nuh in his own session, three by Jacob on the
purchases, three by Jacob today.

**So the honest report is not "fixed". It is: the cause is identified and tested, two mechanical
guards are in place, one recorded recommendation that had been ignored is applied, and the
disposition that produced all nine is a real thing to watch for rather than something a commit can
close.** **Anything stated here as fact that was not checked should be treated as the tenth instance
and reported.**

---

## 18. Session close

**Documentation, tooling and diagnosis only. Nothing was powered. No hardware measurement was
taken.** The only measurement of any kind came out of a mesh: the preamp box's M2 pilot, 5.00 mm
deep and blind, which decides the screw length.

### 18.1 What this session was actually for

It began as "which solder do we buy" and became a four-pass investigation into why the answers had
got worse. **The bench state of the instrument is unchanged. What changed is the repository's
ability to describe itself honestly, and two mechanical guards that did not exist this morning.**

### 18.2 Bench-relevant outcomes, for whoever picks this up

| | |
|---|---|
| **Flux** | Chip Quik RMA791 rosin jar **plus a syringe dispenser**, arriving 2026-09-11. Load the syringe from the jar |
| **Screws** | **M2x6. NOT M2x8** — the box pilot is 5.00 mm deep and blind; an M2x8 bottoms out and cracks the boss. Calipers on the board first |
| **Heat shrink** | Arriving. **Check whether it is adhesive-lined. None at the input node** either way |
| **BLOCKER** | **The cleaning supplies were not ordered.** No 99% IPA, distilled water, brushes, lint-free wipes or foam swabs. **Do not solder the preamp until they are in the room** — rosin left on that board is a leakage path at the node the 119 nA fault sits on |
| **Also unbought** | **Copper tape** with conductive adhesive, for the shield |
| **Piezo** | **Built and working.** Closed. Do not buy Sn42/Bi58 paste, do not re-open the seat-versus-disc question |

### 18.3 Tooling added today

| What | Where |
|---|---|
| **Commit refused while `check_facts.py` fails** | `.githooks/pre-commit`, auto-installed by the session hook via `core.hooksPath` — **reaches Nuh's computer with nobody running anything** |
| **Checker detects its own blind spots** | `check_dead_qualifiers()` — a RETIRED row whose qualifier matches no real wording is reported instead of silently passing |
| **Safety-rule citations verified** | Resolves **and** matches the rule's own wording |
| **Session index verified** | Every log must appear in `sessions/README.md` |
| **Start-up hook, four defects fixed** | Wrong log named, truncated header, hardcoded branch, wrong cause reported |

### 18.4 Corrections to earlier documents

**Recorded here because this is what stops the other computer acting on a stale finding.**

| Where | It said | Correct |
|---|---|---|
| `STATUS.md` ×2 | "safety rule 10" for the DAC modulo-wrap | **Rule 13.** Rules were inserted above it and the citations never moved |
| `CLAUDE.md` §4 | A numbered list of seven, "these are in `STATUS.md` too" | **A different list under different numbers.** Now unnumbered; `STATUS.md` is the only citable one |
| `CLAUDE.md` §3bb, §6 | "Never restate a number outside `FACTS.md`" | **Unfollowable** — `10.24 V` is in 11 live documents. The 2026-09-08 audit had already decided against it |
| `sessions/README.md` | "One file per work session, named `YYYY-MM-DD.md`" | **Four logs have broken that since the first session.** Eight mechanisms were written against the singular form |
| `CLAUDE.md` §5, `/wrap` | "If a file for today already exists, add to it" | **Check whose it is first.** Today's existing file was Nuh's |
| `/wrap` | "If a fault was fixed, remove it" | **Strike through and keep.** Twice here a retraction was itself wrong |
| `sessions/README.md` ×3 | `37 nA` as the offset, read as current | **Superseded, ~119 nA.** Found only once the checker stopped skipping that directory |
| `STATUS.md` | The instruction-file defects explained the quality drop | **They did not.** Mis-attribution, corrected in place — §17 |

### 18.5 False trails

- **The repository was not the cause of the quality drop.** Three passes assumed it was. The hook
  named the *right* log for the question asked, and the answer was **the first row of a table
  printed in full moments earlier.**
- **`CLAUDE.md`'s length was not the cause.** It doubled in four days and that looked obvious. Every
  defect found was a statement that had become **untrue**, not a long one.
- **A provenance checker for `docs/INVENTORY.md` was considered and deliberately not built.** The
  rows without a mark are legitimately "we do not own this" or UNKNOWN, so it would have raised
  false alarms while looking like progress. **Checked before building, which is the discipline that
  failed everywhere else today.**

### 18.6 Process lessons

**The diagnosis, in one line: nine errors across three sessions and both people's work, all the
same move — act on the first plausible reading, write it in as fact, skip the cheap check that
would have disconfirmed it.** Ruled out by evidence, not assumption: the model (`claude-opus-5`,
effort high, no fallback, **checked on two separate days**), context exhaustion (318k of 1M, and
the misread was the session's first exchange), the repository, and file length.

**Four written rules already said "do not infer" and were broken anyway.** So the additions are
mechanism where mechanism exists, and exactly one new piece of prose — **state the interpretation
before spending effort on a purchase, a hardware instruction, or a claim about physical reality.**
That is different in kind from the rest: it does not ask for better judgement, it exposes the
judgement so someone else can check it.

**What cannot be fixed from inside, stated plainly so nobody expects otherwise:** no script can ask
whose board is in a photograph, or which row of the buy list was meant. **All nine errors were
caught by Jacob or Nuh. That remains the real protection, and it worked every time.**

### 18.7 Next session

**Unchanged at the bench.** `docs/NEXT_SESSION_PLAN.md` is still the authority. In short: the
unpowered C2 polarity check first, then the measurement chain — `PREAMP−` is floating and the ADC
has been operated outside its specified input range for the life of the project.

**Do not do:** solder the preamp before the cleaning kit arrives. Use an M2x8 in the preamp box.
Re-open the piezo. Buy Sn42/Bi58 paste. Cite "safety rule N" against `CLAUDE.md` §4.

---

## 19. The bench plan already existed, and nothing had ever updated it

**Jacob asked for a "next bench session" file that keeps itself current, and whether one exists.**

**It exists.** `docs/NEXT_SESSION_PLAN.md`, written 2026-09-07, and `CLAUDE.md` §6 already names it
the canonical home for "what to do next at the bench". **Credit where it is due — it was written by
the 2026-09-07 session and it is a good document.** Presenting it as new would have been the §3b
failure this repository keeps warning about.

**The real answer to his question is the second half: nothing has ever kept it current.**

| Checked | Result |
|---|---|
| Does `/wrap` mention it? | **No** |
| Does `/catchup` mention it? | **No** |
| Commits ever touching it | **3**, and the most recent only edited it incidentally |
| Age of its actual content | **2026-09-07** — two days and three sessions stale |

**That is why neither Jacob nor Nuh knew it was there.** A file nothing writes to is a file nobody
reads.

### 19.1 What had gone stale, and one of them would have cost a print

- Header still read *"for the next session before Sunday"* and *"board rebuild is scheduled for
  Sunday"* — a calendar reference, when the real gate is the floating `PREAMP−`.
- **V3 still asked for callipers on the piezo disc and its pocket.** The piezo is built and working.
- **H1 told you to print the ORIGINAL box base — then described the expected result as the v2
  geometry it forbids two lines earlier.** It claimed **three** Ø1.600 mm pilots "including the new
  one at (4.04, 9.12)". **The original has TWO**, at (5.63, 14.83) and (5.63, 3.40), plus a Ø3.600
  post at (17.70, 9.12) — measured with `stl_features.py`, fit error 0.0001.

> **That last one is the dangerous kind.** Someone printing the correct file and checking it against
> "Expected" would judge a good print faulty — and the obvious recovery is to print v2, which puts a
> carbon-fibre pillar under the input node, the most sensitive point on the instrument. **The same
> section forbids exactly that, two lines above.**

### 19.2 What was done

**Refreshed the plan** and added two gates at the top: the **cleaning-supplies blocker** and
**M2×6 not M2×8**. Fixed H1, closed V3, removed the calendar framing.

**Wired it into the workflow, which is what Jacob actually asked for:**

| Where | Change |
|---|---|
| `/wrap` | **New step 3: update the plan.** Push is now step 5 |
| `CLAUDE.md` §5 | Same step, same place. "Do all **five** steps" |
| `/catchup` | Now reads the plan at the start of a session |
| `check_facts.py` | **`check_plan_freshness()` fails if any session log is newer than the plan's `Last updated` line** |

**The checker is what makes "continuously updates" true rather than aspirational** — and because the
pre-commit hook refuses a commit while the checker fails, **a session that changes something and
does not update the plan cannot commit.**

### 19.3 A fix that was wrong, caught by its own regression test

Refreshing the plan tripped the safety-rule checker: a **correct** citation of rule 0b was flagged,
because the claim was on one line and the citation wrapped onto the next.

**The obvious fix — widen the comparison to a ±1 line window — was made, tested, and reverted.**
The regression showed it **lost one of the two known miscitations and introduced a different false
positive**. Widening a window to clear a false positive, and blunting the check doing it, is
precisely the mistake made yesterday with the `EXCUSES` regex.

**Reverted, with the reason recorded in the code so nobody retries it.** The citation was moved next
to the claim it supports instead — which is what the check was asking for, and better writing.

> **This is `CLAUDE.md` §7 rule 2 doing its job.** The fix looked right and the repository stayed
> green; only re-introducing the fault deliberately showed it had gone blind.

### 19.4 Verified

| Test | Result |
|---|---|
| Plan dated older than the newest session log | **fails**, names both dates |
| Plan with no `Last updated` line | **fails** |
| Repository as it stands | clean, exit 0 |
| Both known safety-rule miscitations, after the revert | **2 of 2 flagged** |
| `stm_approach_test.py` | 40 pass |
| Both shell scripts | `bash -n` clean |
